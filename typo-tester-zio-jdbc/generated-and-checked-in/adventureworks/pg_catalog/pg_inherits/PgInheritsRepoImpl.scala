/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pg_catalog
package pg_inherits

import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

object PgInheritsRepoImpl extends PgInheritsRepo {
  override def delete(compositeId: PgInheritsId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from pg_catalog.pg_inherits where "inhrelid" = ${Segment.paramSegment(compositeId.inhrelid)(Setter.longSetter)} AND "inhseqno" = ${Segment.paramSegment(compositeId.inhseqno)(Setter.intSetter)}""".delete.map(_ > 0)
  }
  override def delete: DeleteBuilder[PgInheritsFields, PgInheritsRow] = {
    DeleteBuilder("pg_catalog.pg_inherits", PgInheritsFields)
  }
  override def insert(unsaved: PgInheritsRow): ZIO[ZConnection, Throwable, PgInheritsRow] = {
    sql"""insert into pg_catalog.pg_inherits("inhrelid", "inhparent", "inhseqno", "inhdetachpending")
          values (${Segment.paramSegment(unsaved.inhrelid)(Setter.longSetter)}::oid, ${Segment.paramSegment(unsaved.inhparent)(Setter.longSetter)}::oid, ${Segment.paramSegment(unsaved.inhseqno)(Setter.intSetter)}::int4, ${Segment.paramSegment(unsaved.inhdetachpending)(Setter.booleanSetter)})
          returning "inhrelid", "inhparent", "inhseqno", "inhdetachpending"
       """.insertReturning(PgInheritsRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def select: SelectBuilder[PgInheritsFields, PgInheritsRow] = {
    SelectBuilderSql("pg_catalog.pg_inherits", PgInheritsFields, PgInheritsRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PgInheritsRow] = {
    sql"""select "inhrelid", "inhparent", "inhseqno", "inhdetachpending" from pg_catalog.pg_inherits""".query(PgInheritsRow.jdbcDecoder).selectStream
  }
  override def selectById(compositeId: PgInheritsId): ZIO[ZConnection, Throwable, Option[PgInheritsRow]] = {
    sql"""select "inhrelid", "inhparent", "inhseqno", "inhdetachpending" from pg_catalog.pg_inherits where "inhrelid" = ${Segment.paramSegment(compositeId.inhrelid)(Setter.longSetter)} AND "inhseqno" = ${Segment.paramSegment(compositeId.inhseqno)(Setter.intSetter)}""".query(PgInheritsRow.jdbcDecoder).selectOne
  }
  override def update(row: PgInheritsRow): ZIO[ZConnection, Throwable, Boolean] = {
    val compositeId = row.compositeId
    sql"""update pg_catalog.pg_inherits
          set "inhparent" = ${Segment.paramSegment(row.inhparent)(Setter.longSetter)}::oid,
              "inhdetachpending" = ${Segment.paramSegment(row.inhdetachpending)(Setter.booleanSetter)}
          where "inhrelid" = ${Segment.paramSegment(compositeId.inhrelid)(Setter.longSetter)} AND "inhseqno" = ${Segment.paramSegment(compositeId.inhseqno)(Setter.intSetter)}""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[PgInheritsFields, PgInheritsRow] = {
    UpdateBuilder("pg_catalog.pg_inherits", PgInheritsFields, PgInheritsRow.jdbcDecoder)
  }
  override def upsert(unsaved: PgInheritsRow): ZIO[ZConnection, Throwable, UpdateResult[PgInheritsRow]] = {
    sql"""insert into pg_catalog.pg_inherits("inhrelid", "inhparent", "inhseqno", "inhdetachpending")
          values (
            ${Segment.paramSegment(unsaved.inhrelid)(Setter.longSetter)}::oid,
            ${Segment.paramSegment(unsaved.inhparent)(Setter.longSetter)}::oid,
            ${Segment.paramSegment(unsaved.inhseqno)(Setter.intSetter)}::int4,
            ${Segment.paramSegment(unsaved.inhdetachpending)(Setter.booleanSetter)}
          )
          on conflict ("inhrelid", "inhseqno")
          do update set
            "inhparent" = EXCLUDED."inhparent",
            "inhdetachpending" = EXCLUDED."inhdetachpending"
          returning "inhrelid", "inhparent", "inhseqno", "inhdetachpending"""".insertReturning(PgInheritsRow.jdbcDecoder)
  }
}
