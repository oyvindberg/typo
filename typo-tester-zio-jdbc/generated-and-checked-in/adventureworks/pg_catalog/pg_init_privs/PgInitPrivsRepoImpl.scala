/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pg_catalog
package pg_init_privs

import adventureworks.customtypes.TypoAclItem
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

object PgInitPrivsRepoImpl extends PgInitPrivsRepo {
  override def delete(compositeId: PgInitPrivsId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from pg_catalog.pg_init_privs where "objoid" = ${Segment.paramSegment(compositeId.objoid)(Setter.longSetter)} AND "classoid" = ${Segment.paramSegment(compositeId.classoid)(Setter.longSetter)} AND "objsubid" = ${Segment.paramSegment(compositeId.objsubid)(Setter.intSetter)}""".delete.map(_ > 0)
  }
  override def delete: DeleteBuilder[PgInitPrivsFields, PgInitPrivsRow] = {
    DeleteBuilder("pg_catalog.pg_init_privs", PgInitPrivsFields)
  }
  override def insert(unsaved: PgInitPrivsRow): ZIO[ZConnection, Throwable, PgInitPrivsRow] = {
    sql"""insert into pg_catalog.pg_init_privs("objoid", "classoid", "objsubid", "privtype", "initprivs")
          values (${Segment.paramSegment(unsaved.objoid)(Setter.longSetter)}::oid, ${Segment.paramSegment(unsaved.classoid)(Setter.longSetter)}::oid, ${Segment.paramSegment(unsaved.objsubid)(Setter.intSetter)}::int4, ${Segment.paramSegment(unsaved.privtype)(Setter.stringSetter)}::char, ${Segment.paramSegment(unsaved.initprivs)(TypoAclItem.arraySetter)}::_aclitem)
          returning "objoid", "classoid", "objsubid", "privtype", "initprivs"
       """.insertReturning(PgInitPrivsRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def select: SelectBuilder[PgInitPrivsFields, PgInitPrivsRow] = {
    SelectBuilderSql("pg_catalog.pg_init_privs", PgInitPrivsFields, PgInitPrivsRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PgInitPrivsRow] = {
    sql"""select "objoid", "classoid", "objsubid", "privtype", "initprivs" from pg_catalog.pg_init_privs""".query(PgInitPrivsRow.jdbcDecoder).selectStream
  }
  override def selectById(compositeId: PgInitPrivsId): ZIO[ZConnection, Throwable, Option[PgInitPrivsRow]] = {
    sql"""select "objoid", "classoid", "objsubid", "privtype", "initprivs" from pg_catalog.pg_init_privs where "objoid" = ${Segment.paramSegment(compositeId.objoid)(Setter.longSetter)} AND "classoid" = ${Segment.paramSegment(compositeId.classoid)(Setter.longSetter)} AND "objsubid" = ${Segment.paramSegment(compositeId.objsubid)(Setter.intSetter)}""".query(PgInitPrivsRow.jdbcDecoder).selectOne
  }
  override def update(row: PgInitPrivsRow): ZIO[ZConnection, Throwable, Boolean] = {
    val compositeId = row.compositeId
    sql"""update pg_catalog.pg_init_privs
          set "privtype" = ${Segment.paramSegment(row.privtype)(Setter.stringSetter)}::char,
              "initprivs" = ${Segment.paramSegment(row.initprivs)(TypoAclItem.arraySetter)}::_aclitem
          where "objoid" = ${Segment.paramSegment(compositeId.objoid)(Setter.longSetter)} AND "classoid" = ${Segment.paramSegment(compositeId.classoid)(Setter.longSetter)} AND "objsubid" = ${Segment.paramSegment(compositeId.objsubid)(Setter.intSetter)}""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[PgInitPrivsFields, PgInitPrivsRow] = {
    UpdateBuilder("pg_catalog.pg_init_privs", PgInitPrivsFields, PgInitPrivsRow.jdbcDecoder)
  }
  override def upsert(unsaved: PgInitPrivsRow): ZIO[ZConnection, Throwable, UpdateResult[PgInitPrivsRow]] = {
    sql"""insert into pg_catalog.pg_init_privs("objoid", "classoid", "objsubid", "privtype", "initprivs")
          values (
            ${Segment.paramSegment(unsaved.objoid)(Setter.longSetter)}::oid,
            ${Segment.paramSegment(unsaved.classoid)(Setter.longSetter)}::oid,
            ${Segment.paramSegment(unsaved.objsubid)(Setter.intSetter)}::int4,
            ${Segment.paramSegment(unsaved.privtype)(Setter.stringSetter)}::char,
            ${Segment.paramSegment(unsaved.initprivs)(TypoAclItem.arraySetter)}::_aclitem
          )
          on conflict ("objoid", "classoid", "objsubid")
          do update set
            "privtype" = EXCLUDED."privtype",
            "initprivs" = EXCLUDED."initprivs"
          returning "objoid", "classoid", "objsubid", "privtype", "initprivs"""".insertReturning(PgInitPrivsRow.jdbcDecoder)
  }
}
