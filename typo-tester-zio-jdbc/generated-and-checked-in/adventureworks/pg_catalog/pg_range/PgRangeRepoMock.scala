/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pg_catalog
package pg_range

import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import zio.Chunk
import zio.ZIO
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.stream.ZStream

class PgRangeRepoMock(map: scala.collection.mutable.Map[PgRangeId, PgRangeRow] = scala.collection.mutable.Map.empty) extends PgRangeRepo {
  override def delete(rngtypid: PgRangeId): ZIO[ZConnection, Throwable, Boolean] = {
    ZIO.succeed(map.remove(rngtypid).isDefined)
  }
  override def delete: DeleteBuilder[PgRangeFields, PgRangeRow] = {
    DeleteBuilderMock(DeleteParams.empty, PgRangeFields, map)
  }
  override def insert(unsaved: PgRangeRow): ZIO[ZConnection, Throwable, PgRangeRow] = {
    ZIO.succeed {
      val _ =
        if (map.contains(unsaved.rngtypid))
          sys.error(s"id ${unsaved.rngtypid} already exists")
        else
          map.put(unsaved.rngtypid, unsaved)
    
      unsaved
    }
  }
  override def select: SelectBuilder[PgRangeFields, PgRangeRow] = {
    SelectBuilderMock(PgRangeFields, ZIO.succeed(Chunk.fromIterable(map.values)), SelectParams.empty)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PgRangeRow] = {
    ZStream.fromIterable(map.values)
  }
  override def selectById(rngtypid: PgRangeId): ZIO[ZConnection, Throwable, Option[PgRangeRow]] = {
    ZIO.succeed(map.get(rngtypid))
  }
  override def selectByIds(rngtypids: Array[PgRangeId]): ZStream[ZConnection, Throwable, PgRangeRow] = {
    ZStream.fromIterable(rngtypids.flatMap(map.get))
  }
  override def selectByUnique(rngmultitypid: /* oid */ Long): ZIO[ZConnection, Throwable, Option[PgRangeRow]] = {
    ZIO.succeed(map.values.find(v => rngmultitypid == v.rngmultitypid))
  }
  override def update(row: PgRangeRow): ZIO[ZConnection, Throwable, Boolean] = {
    ZIO.succeed {
      map.get(row.rngtypid) match {
        case Some(`row`) => false
        case Some(_) =>
          map.put(row.rngtypid, row): @nowarn
          true
        case None => false
      }
    }
  }
  override def update: UpdateBuilder[PgRangeFields, PgRangeRow] = {
    UpdateBuilderMock(UpdateParams.empty, PgRangeFields, map)
  }
  override def upsert(unsaved: PgRangeRow): ZIO[ZConnection, Throwable, UpdateResult[PgRangeRow]] = {
    ZIO.succeed {
      map.put(unsaved.rngtypid, unsaved): @nowarn
      UpdateResult(1, Chunk.single(unsaved))
    }
  }
}
