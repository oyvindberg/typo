/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pg_catalog
package pg_index

import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams
import zio.Chunk
import zio.ZIO
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.stream.ZStream

class PgIndexRepoMock(map: scala.collection.mutable.Map[PgIndexId, PgIndexRow] = scala.collection.mutable.Map.empty) extends PgIndexRepo {
  override def delete(indexrelid: PgIndexId): ZIO[ZConnection, Throwable, Boolean] = {
    ZIO.succeed(map.remove(indexrelid).isDefined)
  }
  override def delete: DeleteBuilder[PgIndexFields, PgIndexRow] = {
    DeleteBuilderMock(DeleteParams.empty, PgIndexFields, map)
  }
  override def insert(unsaved: PgIndexRow): ZIO[ZConnection, Throwable, PgIndexRow] = {
    ZIO.succeed {
      val _ =
        if (map.contains(unsaved.indexrelid))
          sys.error(s"id ${unsaved.indexrelid} already exists")
        else
          map.put(unsaved.indexrelid, unsaved)
    
      unsaved
    }
  }
  override def select: SelectBuilder[PgIndexFields, PgIndexRow] = {
    SelectBuilderMock(PgIndexFields, ZIO.succeed(Chunk.fromIterable(map.values)), SelectParams.empty)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PgIndexRow] = {
    ZStream.fromIterable(map.values)
  }
  override def selectById(indexrelid: PgIndexId): ZIO[ZConnection, Throwable, Option[PgIndexRow]] = {
    ZIO.succeed(map.get(indexrelid))
  }
  override def selectByIds(indexrelids: Array[PgIndexId]): ZStream[ZConnection, Throwable, PgIndexRow] = {
    ZStream.fromIterable(indexrelids.flatMap(map.get))
  }
  override def update(row: PgIndexRow): ZIO[ZConnection, Throwable, Boolean] = {
    ZIO.succeed {
      map.get(row.indexrelid) match {
        case Some(`row`) => false
        case Some(_) =>
          map.put(row.indexrelid, row): @nowarn
          true
        case None => false
      }
    }
  }
  override def update: UpdateBuilder[PgIndexFields, PgIndexRow] = {
    UpdateBuilderMock(UpdateParams.empty, PgIndexFields, map)
  }
  override def upsert(unsaved: PgIndexRow): ZIO[ZConnection, Throwable, UpdateResult[PgIndexRow]] = {
    ZIO.succeed {
      map.put(unsaved.indexrelid, unsaved): @nowarn
      UpdateResult(1, Chunk.single(unsaved))
    }
  }
}
