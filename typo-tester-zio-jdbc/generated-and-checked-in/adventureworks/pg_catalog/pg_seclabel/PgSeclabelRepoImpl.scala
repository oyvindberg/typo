/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pg_catalog
package pg_seclabel

import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

object PgSeclabelRepoImpl extends PgSeclabelRepo {
  override def delete(compositeId: PgSeclabelId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from pg_catalog.pg_seclabel where "objoid" = ${Segment.paramSegment(compositeId.objoid)(Setter.longSetter)} AND "classoid" = ${Segment.paramSegment(compositeId.classoid)(Setter.longSetter)} AND "objsubid" = ${Segment.paramSegment(compositeId.objsubid)(Setter.intSetter)} AND "provider" = ${Segment.paramSegment(compositeId.provider)(Setter.stringSetter)}""".delete.map(_ > 0)
  }
  override def delete: DeleteBuilder[PgSeclabelFields, PgSeclabelRow] = {
    DeleteBuilder("pg_catalog.pg_seclabel", PgSeclabelFields)
  }
  override def insert(unsaved: PgSeclabelRow): ZIO[ZConnection, Throwable, PgSeclabelRow] = {
    sql"""insert into pg_catalog.pg_seclabel("objoid", "classoid", "objsubid", "provider", "label")
          values (${Segment.paramSegment(unsaved.objoid)(Setter.longSetter)}::oid, ${Segment.paramSegment(unsaved.classoid)(Setter.longSetter)}::oid, ${Segment.paramSegment(unsaved.objsubid)(Setter.intSetter)}::int4, ${Segment.paramSegment(unsaved.provider)(Setter.stringSetter)}, ${Segment.paramSegment(unsaved.label)(Setter.stringSetter)})
          returning "objoid", "classoid", "objsubid", "provider", "label"
       """.insertReturning(PgSeclabelRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def select: SelectBuilder[PgSeclabelFields, PgSeclabelRow] = {
    SelectBuilderSql("pg_catalog.pg_seclabel", PgSeclabelFields, PgSeclabelRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PgSeclabelRow] = {
    sql"""select "objoid", "classoid", "objsubid", "provider", "label" from pg_catalog.pg_seclabel""".query(PgSeclabelRow.jdbcDecoder).selectStream
  }
  override def selectById(compositeId: PgSeclabelId): ZIO[ZConnection, Throwable, Option[PgSeclabelRow]] = {
    sql"""select "objoid", "classoid", "objsubid", "provider", "label" from pg_catalog.pg_seclabel where "objoid" = ${Segment.paramSegment(compositeId.objoid)(Setter.longSetter)} AND "classoid" = ${Segment.paramSegment(compositeId.classoid)(Setter.longSetter)} AND "objsubid" = ${Segment.paramSegment(compositeId.objsubid)(Setter.intSetter)} AND "provider" = ${Segment.paramSegment(compositeId.provider)(Setter.stringSetter)}""".query(PgSeclabelRow.jdbcDecoder).selectOne
  }
  override def update(row: PgSeclabelRow): ZIO[ZConnection, Throwable, Boolean] = {
    val compositeId = row.compositeId
    sql"""update pg_catalog.pg_seclabel
          set "label" = ${Segment.paramSegment(row.label)(Setter.stringSetter)}
          where "objoid" = ${Segment.paramSegment(compositeId.objoid)(Setter.longSetter)} AND "classoid" = ${Segment.paramSegment(compositeId.classoid)(Setter.longSetter)} AND "objsubid" = ${Segment.paramSegment(compositeId.objsubid)(Setter.intSetter)} AND "provider" = ${Segment.paramSegment(compositeId.provider)(Setter.stringSetter)}""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[PgSeclabelFields, PgSeclabelRow] = {
    UpdateBuilder("pg_catalog.pg_seclabel", PgSeclabelFields, PgSeclabelRow.jdbcDecoder)
  }
  override def upsert(unsaved: PgSeclabelRow): ZIO[ZConnection, Throwable, UpdateResult[PgSeclabelRow]] = {
    sql"""insert into pg_catalog.pg_seclabel("objoid", "classoid", "objsubid", "provider", "label")
          values (
            ${Segment.paramSegment(unsaved.objoid)(Setter.longSetter)}::oid,
            ${Segment.paramSegment(unsaved.classoid)(Setter.longSetter)}::oid,
            ${Segment.paramSegment(unsaved.objsubid)(Setter.intSetter)}::int4,
            ${Segment.paramSegment(unsaved.provider)(Setter.stringSetter)},
            ${Segment.paramSegment(unsaved.label)(Setter.stringSetter)}
          )
          on conflict ("objoid", "classoid", "objsubid", "provider")
          do update set
            "label" = EXCLUDED."label"
          returning "objoid", "classoid", "objsubid", "provider", "label"""".insertReturning(PgSeclabelRow.jdbcDecoder)
  }
}
