/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package employee

import adventureworks.customtypes.Defaulted
import adventureworks.frontpage.person.PersonId
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class EmployeeRepoImpl extends EmployeeRepo {
  override def delete: DeleteBuilder[EmployeeFields, EmployeeRow] = {
    DeleteBuilder(""""frontpage"."employee"""", EmployeeFields.structure)
  }
  override def deleteById(id: EmployeeId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "frontpage"."employee" where "id" = ${Segment.paramSegment(id)(EmployeeId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(ids: Array[EmployeeId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "frontpage"."employee" where "id" = ANY(${ids})""".delete
  }
  override def insert(unsaved: EmployeeRow): ZIO[ZConnection, Throwable, EmployeeRow] = {
    sql"""insert into "frontpage"."employee"("id", "person_id", "salary")
          values (${Segment.paramSegment(unsaved.id)(EmployeeId.setter)}::uuid, ${Segment.paramSegment(unsaved.personId)(PersonId.setter)}::uuid, ${Segment.paramSegment(unsaved.salary)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric)
          returning "id", "person_id", "salary"
       """.insertReturning(using EmployeeRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: EmployeeRowUnsaved): ZIO[ZConnection, Throwable, EmployeeRow] = {
    val fs = List(
      Some((sql""""person_id"""", sql"${Segment.paramSegment(unsaved.personId)(PersonId.setter)}::uuid")),
      Some((sql""""salary"""", sql"${Segment.paramSegment(unsaved.salary)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""id"""", sql"${Segment.paramSegment(value: EmployeeId)(EmployeeId.setter)}::uuid"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "frontpage"."employee" default values
            returning "id", "person_id", "salary"
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "frontpage"."employee"($names) values ($values) returning "id", "person_id", "salary""""
    }
    q.insertReturning(using EmployeeRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, EmployeeRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."employee"("id", "person_id", "salary") FROM STDIN""", batchSize, unsaved)(EmployeeRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, EmployeeRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."employee"("person_id", "salary", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(EmployeeRowUnsaved.text)
  }
  override def select: SelectBuilder[EmployeeFields, EmployeeRow] = {
    SelectBuilderSql(""""frontpage"."employee"""", EmployeeFields.structure, EmployeeRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, EmployeeRow] = {
    sql"""select "id", "person_id", "salary" from "frontpage"."employee"""".query(using EmployeeRow.jdbcDecoder).selectStream()
  }
  override def selectById(id: EmployeeId): ZIO[ZConnection, Throwable, Option[EmployeeRow]] = {
    sql"""select "id", "person_id", "salary" from "frontpage"."employee" where "id" = ${Segment.paramSegment(id)(EmployeeId.setter)}""".query(using EmployeeRow.jdbcDecoder).selectOne
  }
  override def selectByIds(ids: Array[EmployeeId]): ZStream[ZConnection, Throwable, EmployeeRow] = {
    sql"""select "id", "person_id", "salary" from "frontpage"."employee" where "id" = ANY(${Segment.paramSegment(ids)(EmployeeId.arraySetter)})""".query(using EmployeeRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(ids: Array[EmployeeId]): ZIO[ZConnection, Throwable, Map[EmployeeId, EmployeeRow]] = {
    selectByIds(ids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def selectByUniquePersonId(personId: PersonId): ZIO[ZConnection, Throwable, Option[EmployeeRow]] = {
    sql"""select "id", "person_id", "salary"
          from "frontpage"."employee"
          where "person_id" = ${Segment.paramSegment(personId)(PersonId.setter)}
       """.query(using EmployeeRow.jdbcDecoder).selectOne
  }
  override def update: UpdateBuilder[EmployeeFields, EmployeeRow] = {
    UpdateBuilder(""""frontpage"."employee"""", EmployeeFields.structure, EmployeeRow.jdbcDecoder)
  }
  override def update(row: EmployeeRow): ZIO[ZConnection, Throwable, Boolean] = {
    val id = row.id
    sql"""update "frontpage"."employee"
          set "person_id" = ${Segment.paramSegment(row.personId)(PersonId.setter)}::uuid,
              "salary" = ${Segment.paramSegment(row.salary)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric
          where "id" = ${Segment.paramSegment(id)(EmployeeId.setter)}""".update.map(_ > 0)
  }
  override def upsert(unsaved: EmployeeRow): ZIO[ZConnection, Throwable, UpdateResult[EmployeeRow]] = {
    sql"""insert into "frontpage"."employee"("id", "person_id", "salary")
          values (
            ${Segment.paramSegment(unsaved.id)(EmployeeId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.personId)(PersonId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.salary)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric
          )
          on conflict ("id")
          do update set
            "person_id" = EXCLUDED."person_id",
            "salary" = EXCLUDED."salary"
          returning "id", "person_id", "salary"""".insertReturning(using EmployeeRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, EmployeeRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table employee_TEMP (like "frontpage"."employee") on commit drop""".execute
    val copied = streamingInsert(s"""copy employee_TEMP("id", "person_id", "salary") from stdin""", batchSize, unsaved)(EmployeeRow.text)
    val merged = sql"""insert into "frontpage"."employee"("id", "person_id", "salary")
                       select * from employee_TEMP
                       on conflict ("id")
                       do update set
                         "person_id" = EXCLUDED."person_id",
                         "salary" = EXCLUDED."salary"
                       ;
                       drop table employee_TEMP;""".update
    created *> copied *> merged
  }
}
