/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package company

import adventureworks.customtypes.Defaulted
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `frontpage.company` which has not been persisted yet */
case class CompanyRowUnsaved(
  name: String,
  /** Default: gen_random_uuid() */
  id: Defaulted[CompanyId] = Defaulted.UseDefault
) {
  def toRow(idDefault: => CompanyId): CompanyRow =
    CompanyRow(
      name = name,
      id = id match {
             case Defaulted.UseDefault => idDefault
             case Defaulted.Provided(value) => value
           }
    )
}
object CompanyRowUnsaved {
  implicit lazy val jsonDecoder: JsonDecoder[CompanyRowUnsaved] = JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
    val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(JsonDecoder.string))
    val id = jsonObj.get("id").toRight("Missing field 'id'").flatMap(_.as(Defaulted.jsonDecoder(CompanyId.jsonDecoder)))
    if (name.isRight && id.isRight)
      Right(CompanyRowUnsaved(name = name.toOption.get, id = id.toOption.get))
    else Left(List[Either[String, Any]](name, id).flatMap(_.left.toOption).mkString(", "))
  }
  implicit lazy val jsonEncoder: JsonEncoder[CompanyRowUnsaved] = new JsonEncoder[CompanyRowUnsaved] {
    override def unsafeEncode(a: CompanyRowUnsaved, indent: Option[Int], out: Write): Unit = {
      out.write("{")
      out.write(""""name":""")
      JsonEncoder.string.unsafeEncode(a.name, indent, out)
      out.write(",")
      out.write(""""id":""")
      Defaulted.jsonEncoder(CompanyId.jsonEncoder).unsafeEncode(a.id, indent, out)
      out.write("}")
    }
  }
  implicit lazy val text: Text[CompanyRowUnsaved] = Text.instance[CompanyRowUnsaved]{ (row, sb) =>
    Text.stringInstance.unsafeEncode(row.name, sb)
    sb.append(Text.DELIMETER)
    Defaulted.text(CompanyId.text).unsafeEncode(row.id, sb)
  }
}
