/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package company

import adventureworks.customtypes.Defaulted
import java.sql.ResultSet
import zio.jdbc.JdbcDecoder
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** Table: frontpage.company
    Primary key: id */
case class CompanyRow(
  /** Default: gen_random_uuid() */
  id: CompanyId,
  name: String
){
   def toUnsavedRow(id: Defaulted[CompanyId]): CompanyRowUnsaved =
     CompanyRowUnsaved(name, id)
 }

object CompanyRow {
  implicit lazy val jdbcDecoder: JdbcDecoder[CompanyRow] = new JdbcDecoder[CompanyRow] {
    override def unsafeDecode(columIndex: Int, rs: ResultSet): (Int, CompanyRow) =
      columIndex + 1 ->
        CompanyRow(
          id = CompanyId.jdbcDecoder.unsafeDecode(columIndex + 0, rs)._2,
          name = JdbcDecoder.stringDecoder.unsafeDecode(columIndex + 1, rs)._2
        )
  }
  implicit lazy val jsonDecoder: JsonDecoder[CompanyRow] = JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
    val id = jsonObj.get("id").toRight("Missing field 'id'").flatMap(_.as(CompanyId.jsonDecoder))
    val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(JsonDecoder.string))
    if (id.isRight && name.isRight)
      Right(CompanyRow(id = id.toOption.get, name = name.toOption.get))
    else Left(List[Either[String, Any]](id, name).flatMap(_.left.toOption).mkString(", "))
  }
  implicit lazy val jsonEncoder: JsonEncoder[CompanyRow] = new JsonEncoder[CompanyRow] {
    override def unsafeEncode(a: CompanyRow, indent: Option[Int], out: Write): Unit = {
      out.write("{")
      out.write(""""id":""")
      CompanyId.jsonEncoder.unsafeEncode(a.id, indent, out)
      out.write(",")
      out.write(""""name":""")
      JsonEncoder.string.unsafeEncode(a.name, indent, out)
      out.write("}")
    }
  }
  implicit lazy val text: Text[CompanyRow] = Text.instance[CompanyRow]{ (row, sb) =>
    CompanyId.text.unsafeEncode(row.id, sb)
    sb.append(Text.DELIMETER)
    Text.stringInstance.unsafeEncode(row.name, sb)
  }
}
