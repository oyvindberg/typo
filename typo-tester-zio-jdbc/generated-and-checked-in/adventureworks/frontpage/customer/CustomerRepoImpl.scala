/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package customer

import adventureworks.customtypes.Defaulted
import adventureworks.frontpage.user.UserId
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class CustomerRepoImpl extends CustomerRepo {
  override def delete: DeleteBuilder[CustomerFields, CustomerRow] = {
    DeleteBuilder(""""frontpage"."customer"""", CustomerFields.structure)
  }
  override def deleteById(id: CustomerId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "frontpage"."customer" where "id" = ${Segment.paramSegment(id)(CustomerId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(ids: Array[CustomerId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "frontpage"."customer" where "id" = ANY(${ids})""".delete
  }
  override def insert(unsaved: CustomerRow): ZIO[ZConnection, Throwable, CustomerRow] = {
    sql"""insert into "frontpage"."customer"("id", "user_id", "company_name", "credit_limit", "verified")
          values (${Segment.paramSegment(unsaved.id)(CustomerId.setter)}::uuid, ${Segment.paramSegment(unsaved.userId)(Setter.optionParamSetter(UserId.setter))}::uuid, ${Segment.paramSegment(unsaved.companyName)(Setter.optionParamSetter(Setter.stringSetter))}, ${Segment.paramSegment(unsaved.creditLimit)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric, ${Segment.paramSegment(unsaved.verified)(Setter.optionParamSetter(Setter.booleanSetter))})
          returning "id", "user_id", "company_name", "credit_limit", "verified"
       """.insertReturning(using CustomerRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: CustomerRowUnsaved): ZIO[ZConnection, Throwable, CustomerRow] = {
    val fs = List(
      Some((sql""""user_id"""", sql"${Segment.paramSegment(unsaved.userId)(Setter.optionParamSetter(UserId.setter))}::uuid")),
      Some((sql""""company_name"""", sql"${Segment.paramSegment(unsaved.companyName)(Setter.optionParamSetter(Setter.stringSetter))}")),
      Some((sql""""credit_limit"""", sql"${Segment.paramSegment(unsaved.creditLimit)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""id"""", sql"${Segment.paramSegment(value: CustomerId)(CustomerId.setter)}::uuid"))
      },
      unsaved.verified match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""verified"""", sql"${Segment.paramSegment(value: Option[Boolean])(Setter.optionParamSetter(Setter.booleanSetter))}"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "frontpage"."customer" default values
            returning "id", "user_id", "company_name", "credit_limit", "verified"
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "frontpage"."customer"($names) values ($values) returning "id", "user_id", "company_name", "credit_limit", "verified""""
    }
    q.insertReturning(using CustomerRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, CustomerRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."customer"("id", "user_id", "company_name", "credit_limit", "verified") FROM STDIN""", batchSize, unsaved)(CustomerRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, CustomerRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."customer"("user_id", "company_name", "credit_limit", "id", "verified") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(CustomerRowUnsaved.text)
  }
  override def select: SelectBuilder[CustomerFields, CustomerRow] = {
    SelectBuilderSql(""""frontpage"."customer"""", CustomerFields.structure, CustomerRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, CustomerRow] = {
    sql"""select "id", "user_id", "company_name", "credit_limit", "verified" from "frontpage"."customer"""".query(using CustomerRow.jdbcDecoder).selectStream()
  }
  override def selectById(id: CustomerId): ZIO[ZConnection, Throwable, Option[CustomerRow]] = {
    sql"""select "id", "user_id", "company_name", "credit_limit", "verified" from "frontpage"."customer" where "id" = ${Segment.paramSegment(id)(CustomerId.setter)}""".query(using CustomerRow.jdbcDecoder).selectOne
  }
  override def selectByIds(ids: Array[CustomerId]): ZStream[ZConnection, Throwable, CustomerRow] = {
    sql"""select "id", "user_id", "company_name", "credit_limit", "verified" from "frontpage"."customer" where "id" = ANY(${Segment.paramSegment(ids)(CustomerId.arraySetter)})""".query(using CustomerRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(ids: Array[CustomerId]): ZIO[ZConnection, Throwable, Map[CustomerId, CustomerRow]] = {
    selectByIds(ids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def update: UpdateBuilder[CustomerFields, CustomerRow] = {
    UpdateBuilder(""""frontpage"."customer"""", CustomerFields.structure, CustomerRow.jdbcDecoder)
  }
  override def update(row: CustomerRow): ZIO[ZConnection, Throwable, Boolean] = {
    val id = row.id
    sql"""update "frontpage"."customer"
          set "user_id" = ${Segment.paramSegment(row.userId)(Setter.optionParamSetter(UserId.setter))}::uuid,
              "company_name" = ${Segment.paramSegment(row.companyName)(Setter.optionParamSetter(Setter.stringSetter))},
              "credit_limit" = ${Segment.paramSegment(row.creditLimit)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric,
              "verified" = ${Segment.paramSegment(row.verified)(Setter.optionParamSetter(Setter.booleanSetter))}
          where "id" = ${Segment.paramSegment(id)(CustomerId.setter)}""".update.map(_ > 0)
  }
  override def upsert(unsaved: CustomerRow): ZIO[ZConnection, Throwable, UpdateResult[CustomerRow]] = {
    sql"""insert into "frontpage"."customer"("id", "user_id", "company_name", "credit_limit", "verified")
          values (
            ${Segment.paramSegment(unsaved.id)(CustomerId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.userId)(Setter.optionParamSetter(UserId.setter))}::uuid,
            ${Segment.paramSegment(unsaved.companyName)(Setter.optionParamSetter(Setter.stringSetter))},
            ${Segment.paramSegment(unsaved.creditLimit)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric,
            ${Segment.paramSegment(unsaved.verified)(Setter.optionParamSetter(Setter.booleanSetter))}
          )
          on conflict ("id")
          do update set
            "user_id" = EXCLUDED."user_id",
            "company_name" = EXCLUDED."company_name",
            "credit_limit" = EXCLUDED."credit_limit",
            "verified" = EXCLUDED."verified"
          returning "id", "user_id", "company_name", "credit_limit", "verified"""".insertReturning(using CustomerRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, CustomerRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table customer_TEMP (like "frontpage"."customer") on commit drop""".execute
    val copied = streamingInsert(s"""copy customer_TEMP("id", "user_id", "company_name", "credit_limit", "verified") from stdin""", batchSize, unsaved)(CustomerRow.text)
    val merged = sql"""insert into "frontpage"."customer"("id", "user_id", "company_name", "credit_limit", "verified")
                       select * from customer_TEMP
                       on conflict ("id")
                       do update set
                         "user_id" = EXCLUDED."user_id",
                         "company_name" = EXCLUDED."company_name",
                         "credit_limit" = EXCLUDED."credit_limit",
                         "verified" = EXCLUDED."verified"
                       ;
                       drop table customer_TEMP;""".update
    created *> copied *> merged
  }
}
