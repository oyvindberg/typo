/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package location

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoInet
import adventureworks.customtypes.TypoJsonb
import adventureworks.customtypes.TypoPoint
import adventureworks.customtypes.TypoPolygon
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class LocationRepoImpl extends LocationRepo {
  override def delete: DeleteBuilder[LocationFields, LocationRow] = {
    DeleteBuilder(""""frontpage"."location"""", LocationFields.structure)
  }
  override def deleteById(id: LocationId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "frontpage"."location" where "id" = ${Segment.paramSegment(id)(LocationId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(ids: Array[LocationId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "frontpage"."location" where "id" = ANY(${ids})""".delete
  }
  override def insert(unsaved: LocationRow): ZIO[ZConnection, Throwable, LocationRow] = {
    sql"""insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
          values (${Segment.paramSegment(unsaved.id)(LocationId.setter)}::uuid, ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}, ${Segment.paramSegment(unsaved.position)(Setter.optionParamSetter(TypoPoint.setter))}::point, ${Segment.paramSegment(unsaved.area)(Setter.optionParamSetter(TypoPolygon.setter))}::polygon, ${Segment.paramSegment(unsaved.ipRange)(Setter.optionParamSetter(TypoInet.setter))}::inet, ${Segment.paramSegment(unsaved.metadata)(Setter.optionParamSetter(TypoJsonb.setter))}::jsonb)
          returning "id", "name", "position", "area", "ip_range", "metadata"
       """.insertReturning(using LocationRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: LocationRowUnsaved): ZIO[ZConnection, Throwable, LocationRow] = {
    val fs = List(
      Some((sql""""name"""", sql"${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}")),
      Some((sql""""position"""", sql"${Segment.paramSegment(unsaved.position)(Setter.optionParamSetter(TypoPoint.setter))}::point")),
      Some((sql""""area"""", sql"${Segment.paramSegment(unsaved.area)(Setter.optionParamSetter(TypoPolygon.setter))}::polygon")),
      Some((sql""""ip_range"""", sql"${Segment.paramSegment(unsaved.ipRange)(Setter.optionParamSetter(TypoInet.setter))}::inet")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""id"""", sql"${Segment.paramSegment(value: LocationId)(LocationId.setter)}::uuid"))
      },
      unsaved.metadata match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""metadata"""", sql"${Segment.paramSegment(value: Option[TypoJsonb])(Setter.optionParamSetter(TypoJsonb.setter))}::jsonb"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "frontpage"."location" default values
            returning "id", "name", "position", "area", "ip_range", "metadata"
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "frontpage"."location"($names) values ($values) returning "id", "name", "position", "area", "ip_range", "metadata""""
    }
    q.insertReturning(using LocationRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, LocationRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata") FROM STDIN""", batchSize, unsaved)(LocationRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, LocationRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."location"("name", "position", "area", "ip_range", "id", "metadata") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(LocationRowUnsaved.text)
  }
  override def select: SelectBuilder[LocationFields, LocationRow] = {
    SelectBuilderSql(""""frontpage"."location"""", LocationFields.structure, LocationRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, LocationRow] = {
    sql"""select "id", "name", "position", "area", "ip_range", "metadata" from "frontpage"."location"""".query(using LocationRow.jdbcDecoder).selectStream()
  }
  override def selectById(id: LocationId): ZIO[ZConnection, Throwable, Option[LocationRow]] = {
    sql"""select "id", "name", "position", "area", "ip_range", "metadata" from "frontpage"."location" where "id" = ${Segment.paramSegment(id)(LocationId.setter)}""".query(using LocationRow.jdbcDecoder).selectOne
  }
  override def selectByIds(ids: Array[LocationId]): ZStream[ZConnection, Throwable, LocationRow] = {
    sql"""select "id", "name", "position", "area", "ip_range", "metadata" from "frontpage"."location" where "id" = ANY(${Segment.paramSegment(ids)(LocationId.arraySetter)})""".query(using LocationRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(ids: Array[LocationId]): ZIO[ZConnection, Throwable, Map[LocationId, LocationRow]] = {
    selectByIds(ids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def update: UpdateBuilder[LocationFields, LocationRow] = {
    UpdateBuilder(""""frontpage"."location"""", LocationFields.structure, LocationRow.jdbcDecoder)
  }
  override def update(row: LocationRow): ZIO[ZConnection, Throwable, Boolean] = {
    val id = row.id
    sql"""update "frontpage"."location"
          set "name" = ${Segment.paramSegment(row.name)(Setter.stringSetter)},
              "position" = ${Segment.paramSegment(row.position)(Setter.optionParamSetter(TypoPoint.setter))}::point,
              "area" = ${Segment.paramSegment(row.area)(Setter.optionParamSetter(TypoPolygon.setter))}::polygon,
              "ip_range" = ${Segment.paramSegment(row.ipRange)(Setter.optionParamSetter(TypoInet.setter))}::inet,
              "metadata" = ${Segment.paramSegment(row.metadata)(Setter.optionParamSetter(TypoJsonb.setter))}::jsonb
          where "id" = ${Segment.paramSegment(id)(LocationId.setter)}""".update.map(_ > 0)
  }
  override def upsert(unsaved: LocationRow): ZIO[ZConnection, Throwable, UpdateResult[LocationRow]] = {
    sql"""insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
          values (
            ${Segment.paramSegment(unsaved.id)(LocationId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)},
            ${Segment.paramSegment(unsaved.position)(Setter.optionParamSetter(TypoPoint.setter))}::point,
            ${Segment.paramSegment(unsaved.area)(Setter.optionParamSetter(TypoPolygon.setter))}::polygon,
            ${Segment.paramSegment(unsaved.ipRange)(Setter.optionParamSetter(TypoInet.setter))}::inet,
            ${Segment.paramSegment(unsaved.metadata)(Setter.optionParamSetter(TypoJsonb.setter))}::jsonb
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "position" = EXCLUDED."position",
            "area" = EXCLUDED."area",
            "ip_range" = EXCLUDED."ip_range",
            "metadata" = EXCLUDED."metadata"
          returning "id", "name", "position", "area", "ip_range", "metadata"""".insertReturning(using LocationRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, LocationRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table location_TEMP (like "frontpage"."location") on commit drop""".execute
    val copied = streamingInsert(s"""copy location_TEMP("id", "name", "position", "area", "ip_range", "metadata") from stdin""", batchSize, unsaved)(LocationRow.text)
    val merged = sql"""insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
                       select * from location_TEMP
                       on conflict ("id")
                       do update set
                         "name" = EXCLUDED."name",
                         "position" = EXCLUDED."position",
                         "area" = EXCLUDED."area",
                         "ip_range" = EXCLUDED."ip_range",
                         "metadata" = EXCLUDED."metadata"
                       ;
                       drop table location_TEMP;""".update
    created *> copied *> merged
  }
}
