/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package category

import adventureworks.customtypes.Defaulted
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `frontpage.category` which has not been persisted yet */
case class CategoryRowUnsaved(
  name: String,
  /** Default: gen_random_uuid() */
  id: Defaulted[CategoryId] = Defaulted.UseDefault
) {
  def toRow(idDefault: => CategoryId): CategoryRow =
    CategoryRow(
      name = name,
      id = id match {
             case Defaulted.UseDefault => idDefault
             case Defaulted.Provided(value) => value
           }
    )
}
object CategoryRowUnsaved {
  implicit lazy val jsonDecoder: JsonDecoder[CategoryRowUnsaved] = JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
    val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(JsonDecoder.string))
    val id = jsonObj.get("id").toRight("Missing field 'id'").flatMap(_.as(Defaulted.jsonDecoder(CategoryId.jsonDecoder)))
    if (name.isRight && id.isRight)
      Right(CategoryRowUnsaved(name = name.toOption.get, id = id.toOption.get))
    else Left(List[Either[String, Any]](name, id).flatMap(_.left.toOption).mkString(", "))
  }
  implicit lazy val jsonEncoder: JsonEncoder[CategoryRowUnsaved] = new JsonEncoder[CategoryRowUnsaved] {
    override def unsafeEncode(a: CategoryRowUnsaved, indent: Option[Int], out: Write): Unit = {
      out.write("{")
      out.write(""""name":""")
      JsonEncoder.string.unsafeEncode(a.name, indent, out)
      out.write(",")
      out.write(""""id":""")
      Defaulted.jsonEncoder(CategoryId.jsonEncoder).unsafeEncode(a.id, indent, out)
      out.write("}")
    }
  }
  implicit lazy val text: Text[CategoryRowUnsaved] = Text.instance[CategoryRowUnsaved]{ (row, sb) =>
    Text.stringInstance.unsafeEncode(row.name, sb)
    sb.append(Text.DELIMETER)
    Defaulted.text(CategoryId.text).unsafeEncode(row.id, sb)
  }
}
