/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package person

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.frontpage.address.AddressId
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class PersonRepoImpl extends PersonRepo {
  override def delete: DeleteBuilder[PersonFields, PersonRow] = {
    DeleteBuilder(""""frontpage"."person"""", PersonFields.structure)
  }
  override def deleteById(id: PersonId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "frontpage"."person" where "id" = ${Segment.paramSegment(id)(PersonId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(ids: Array[PersonId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "frontpage"."person" where "id" = ANY(${ids})""".delete
  }
  override def insert(unsaved: PersonRow): ZIO[ZConnection, Throwable, PersonRow] = {
    sql"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
          values (${Segment.paramSegment(unsaved.id)(PersonId.setter)}::uuid, ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}, ${Segment.paramSegment(unsaved.addressId)(Setter.optionParamSetter(AddressId.setter))}::uuid, ${Segment.paramSegment(unsaved.createdAt)(Setter.optionParamSetter(TypoLocalDateTime.setter))}::timestamp)
          returning "id", "name", "address_id", "created_at"::text
       """.insertReturning(using PersonRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: PersonRowUnsaved): ZIO[ZConnection, Throwable, PersonRow] = {
    val fs = List(
      Some((sql""""name"""", sql"${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}")),
      Some((sql""""address_id"""", sql"${Segment.paramSegment(unsaved.addressId)(Setter.optionParamSetter(AddressId.setter))}::uuid")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""id"""", sql"${Segment.paramSegment(value: PersonId)(PersonId.setter)}::uuid"))
      },
      unsaved.createdAt match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""created_at"""", sql"${Segment.paramSegment(value: Option[TypoLocalDateTime])(Setter.optionParamSetter(TypoLocalDateTime.setter))}::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "frontpage"."person" default values
            returning "id", "name", "address_id", "created_at"::text
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "frontpage"."person"($names) values ($values) returning "id", "name", "address_id", "created_at"::text"""
    }
    q.insertReturning(using PersonRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, PersonRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."person"("id", "name", "address_id", "created_at") FROM STDIN""", batchSize, unsaved)(PersonRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, PersonRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."person"("name", "address_id", "id", "created_at") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(PersonRowUnsaved.text)
  }
  override def select: SelectBuilder[PersonFields, PersonRow] = {
    SelectBuilderSql(""""frontpage"."person"""", PersonFields.structure, PersonRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PersonRow] = {
    sql"""select "id", "name", "address_id", "created_at"::text from "frontpage"."person"""".query(using PersonRow.jdbcDecoder).selectStream()
  }
  override def selectById(id: PersonId): ZIO[ZConnection, Throwable, Option[PersonRow]] = {
    sql"""select "id", "name", "address_id", "created_at"::text from "frontpage"."person" where "id" = ${Segment.paramSegment(id)(PersonId.setter)}""".query(using PersonRow.jdbcDecoder).selectOne
  }
  override def selectByIds(ids: Array[PersonId]): ZStream[ZConnection, Throwable, PersonRow] = {
    sql"""select "id", "name", "address_id", "created_at"::text from "frontpage"."person" where "id" = ANY(${Segment.paramSegment(ids)(PersonId.arraySetter)})""".query(using PersonRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(ids: Array[PersonId]): ZIO[ZConnection, Throwable, Map[PersonId, PersonRow]] = {
    selectByIds(ids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def update: UpdateBuilder[PersonFields, PersonRow] = {
    UpdateBuilder(""""frontpage"."person"""", PersonFields.structure, PersonRow.jdbcDecoder)
  }
  override def update(row: PersonRow): ZIO[ZConnection, Throwable, Boolean] = {
    val id = row.id
    sql"""update "frontpage"."person"
          set "name" = ${Segment.paramSegment(row.name)(Setter.stringSetter)},
              "address_id" = ${Segment.paramSegment(row.addressId)(Setter.optionParamSetter(AddressId.setter))}::uuid,
              "created_at" = ${Segment.paramSegment(row.createdAt)(Setter.optionParamSetter(TypoLocalDateTime.setter))}::timestamp
          where "id" = ${Segment.paramSegment(id)(PersonId.setter)}""".update.map(_ > 0)
  }
  override def upsert(unsaved: PersonRow): ZIO[ZConnection, Throwable, UpdateResult[PersonRow]] = {
    sql"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
          values (
            ${Segment.paramSegment(unsaved.id)(PersonId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)},
            ${Segment.paramSegment(unsaved.addressId)(Setter.optionParamSetter(AddressId.setter))}::uuid,
            ${Segment.paramSegment(unsaved.createdAt)(Setter.optionParamSetter(TypoLocalDateTime.setter))}::timestamp
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "address_id" = EXCLUDED."address_id",
            "created_at" = EXCLUDED."created_at"
          returning "id", "name", "address_id", "created_at"::text""".insertReturning(using PersonRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, PersonRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table person_TEMP (like "frontpage"."person") on commit drop""".execute
    val copied = streamingInsert(s"""copy person_TEMP("id", "name", "address_id", "created_at") from stdin""", batchSize, unsaved)(PersonRow.text)
    val merged = sql"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
                       select * from person_TEMP
                       on conflict ("id")
                       do update set
                         "name" = EXCLUDED."name",
                         "address_id" = EXCLUDED."address_id",
                         "created_at" = EXCLUDED."created_at"
                       ;
                       drop table person_TEMP;""".update
    created *> copied *> merged
  }
}
