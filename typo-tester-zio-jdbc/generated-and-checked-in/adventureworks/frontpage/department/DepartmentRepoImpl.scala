/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package department

import adventureworks.customtypes.Defaulted
import adventureworks.frontpage.company.CompanyId
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class DepartmentRepoImpl extends DepartmentRepo {
  override def delete: DeleteBuilder[DepartmentFields, DepartmentRow] = {
    DeleteBuilder(""""frontpage"."department"""", DepartmentFields.structure)
  }
  override def deleteById(id: DepartmentId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "frontpage"."department" where "id" = ${Segment.paramSegment(id)(DepartmentId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(ids: Array[DepartmentId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "frontpage"."department" where "id" = ANY(${ids})""".delete
  }
  override def insert(unsaved: DepartmentRow): ZIO[ZConnection, Throwable, DepartmentRow] = {
    sql"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          values (${Segment.paramSegment(unsaved.id)(DepartmentId.setter)}::uuid, ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}, ${Segment.paramSegment(unsaved.budget)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric, ${Segment.paramSegment(unsaved.companyId)(Setter.optionParamSetter(CompanyId.setter))}::uuid)
          returning "id", "name", "budget", "company_id"
       """.insertReturning(using DepartmentRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: DepartmentRowUnsaved): ZIO[ZConnection, Throwable, DepartmentRow] = {
    val fs = List(
      Some((sql""""name"""", sql"${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}")),
      Some((sql""""budget"""", sql"${Segment.paramSegment(unsaved.budget)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric")),
      Some((sql""""company_id"""", sql"${Segment.paramSegment(unsaved.companyId)(Setter.optionParamSetter(CompanyId.setter))}::uuid")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""id"""", sql"${Segment.paramSegment(value: DepartmentId)(DepartmentId.setter)}::uuid"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "frontpage"."department" default values
            returning "id", "name", "budget", "company_id"
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "frontpage"."department"($names) values ($values) returning "id", "name", "budget", "company_id""""
    }
    q.insertReturning(using DepartmentRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, DepartmentRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."department"("id", "name", "budget", "company_id") FROM STDIN""", batchSize, unsaved)(DepartmentRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, DepartmentRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."department"("name", "budget", "company_id", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(DepartmentRowUnsaved.text)
  }
  override def select: SelectBuilder[DepartmentFields, DepartmentRow] = {
    SelectBuilderSql(""""frontpage"."department"""", DepartmentFields.structure, DepartmentRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, DepartmentRow] = {
    sql"""select "id", "name", "budget", "company_id" from "frontpage"."department"""".query(using DepartmentRow.jdbcDecoder).selectStream()
  }
  override def selectById(id: DepartmentId): ZIO[ZConnection, Throwable, Option[DepartmentRow]] = {
    sql"""select "id", "name", "budget", "company_id" from "frontpage"."department" where "id" = ${Segment.paramSegment(id)(DepartmentId.setter)}""".query(using DepartmentRow.jdbcDecoder).selectOne
  }
  override def selectByIds(ids: Array[DepartmentId]): ZStream[ZConnection, Throwable, DepartmentRow] = {
    sql"""select "id", "name", "budget", "company_id" from "frontpage"."department" where "id" = ANY(${Segment.paramSegment(ids)(DepartmentId.arraySetter)})""".query(using DepartmentRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(ids: Array[DepartmentId]): ZIO[ZConnection, Throwable, Map[DepartmentId, DepartmentRow]] = {
    selectByIds(ids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def update: UpdateBuilder[DepartmentFields, DepartmentRow] = {
    UpdateBuilder(""""frontpage"."department"""", DepartmentFields.structure, DepartmentRow.jdbcDecoder)
  }
  override def update(row: DepartmentRow): ZIO[ZConnection, Throwable, Boolean] = {
    val id = row.id
    sql"""update "frontpage"."department"
          set "name" = ${Segment.paramSegment(row.name)(Setter.stringSetter)},
              "budget" = ${Segment.paramSegment(row.budget)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric,
              "company_id" = ${Segment.paramSegment(row.companyId)(Setter.optionParamSetter(CompanyId.setter))}::uuid
          where "id" = ${Segment.paramSegment(id)(DepartmentId.setter)}""".update.map(_ > 0)
  }
  override def upsert(unsaved: DepartmentRow): ZIO[ZConnection, Throwable, UpdateResult[DepartmentRow]] = {
    sql"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          values (
            ${Segment.paramSegment(unsaved.id)(DepartmentId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)},
            ${Segment.paramSegment(unsaved.budget)(Setter.optionParamSetter(Setter.bigDecimalScalaSetter))}::numeric,
            ${Segment.paramSegment(unsaved.companyId)(Setter.optionParamSetter(CompanyId.setter))}::uuid
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "budget" = EXCLUDED."budget",
            "company_id" = EXCLUDED."company_id"
          returning "id", "name", "budget", "company_id"""".insertReturning(using DepartmentRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, DepartmentRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table department_TEMP (like "frontpage"."department") on commit drop""".execute
    val copied = streamingInsert(s"""copy department_TEMP("id", "name", "budget", "company_id") from stdin""", batchSize, unsaved)(DepartmentRow.text)
    val merged = sql"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
                       select * from department_TEMP
                       on conflict ("id")
                       do update set
                         "name" = EXCLUDED."name",
                         "budget" = EXCLUDED."budget",
                         "company_id" = EXCLUDED."company_id"
                       ;
                       drop table department_TEMP;""".update
    created *> copied *> merged
  }
}
