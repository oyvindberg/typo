/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package permission

import adventureworks.customtypes.Defaulted
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlFragment.Segment
import zio.jdbc.SqlFragment.Setter
import zio.jdbc.UpdateResult
import zio.jdbc.ZConnection
import zio.jdbc.sqlInterpolator
import zio.stream.ZStream

class PermissionRepoImpl extends PermissionRepo {
  override def delete: DeleteBuilder[PermissionFields, PermissionRow] = {
    DeleteBuilder(""""frontpage"."permission"""", PermissionFields.structure)
  }
  override def deleteById(id: PermissionId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from "frontpage"."permission" where "id" = ${Segment.paramSegment(id)(PermissionId.setter)}""".delete.map(_ > 0)
  }
  override def deleteByIds(ids: Array[PermissionId]): ZIO[ZConnection, Throwable, Long] = {
    sql"""delete from "frontpage"."permission" where "id" = ANY(${ids})""".delete
  }
  override def insert(unsaved: PermissionRow): ZIO[ZConnection, Throwable, PermissionRow] = {
    sql"""insert into "frontpage"."permission"("id", "name")
          values (${Segment.paramSegment(unsaved.id)(PermissionId.setter)}::uuid, ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)})
          returning "id", "name"
       """.insertReturning(using PermissionRow.jdbcDecoder).map(_.updatedKeys.head)
  }
  override def insert(unsaved: PermissionRowUnsaved): ZIO[ZConnection, Throwable, PermissionRow] = {
    val fs = List(
      Some((sql""""name"""", sql"${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((sql""""id"""", sql"${Segment.paramSegment(value: PermissionId)(PermissionId.setter)}::uuid"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "frontpage"."permission" default values
            returning "id", "name"
         """
    } else {
      val names  = fs.map { case (n, _) => n }.mkFragment(SqlFragment(", "))
      val values = fs.map { case (_, f) => f }.mkFragment(SqlFragment(", "))
      sql"""insert into "frontpage"."permission"($names) values ($values) returning "id", "name""""
    }
    q.insertReturning(using PermissionRow.jdbcDecoder).map(_.updatedKeys.head)
    
  }
  override def insertStreaming(unsaved: ZStream[ZConnection, Throwable, PermissionRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."permission"("id", "name") FROM STDIN""", batchSize, unsaved)(PermissionRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: ZStream[ZConnection, Throwable, PermissionRowUnsaved], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    streamingInsert(s"""COPY "frontpage"."permission"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(PermissionRowUnsaved.text)
  }
  override def select: SelectBuilder[PermissionFields, PermissionRow] = {
    SelectBuilderSql(""""frontpage"."permission"""", PermissionFields.structure, PermissionRow.jdbcDecoder)
  }
  override def selectAll: ZStream[ZConnection, Throwable, PermissionRow] = {
    sql"""select "id", "name" from "frontpage"."permission"""".query(using PermissionRow.jdbcDecoder).selectStream()
  }
  override def selectById(id: PermissionId): ZIO[ZConnection, Throwable, Option[PermissionRow]] = {
    sql"""select "id", "name" from "frontpage"."permission" where "id" = ${Segment.paramSegment(id)(PermissionId.setter)}""".query(using PermissionRow.jdbcDecoder).selectOne
  }
  override def selectByIds(ids: Array[PermissionId]): ZStream[ZConnection, Throwable, PermissionRow] = {
    sql"""select "id", "name" from "frontpage"."permission" where "id" = ANY(${Segment.paramSegment(ids)(PermissionId.arraySetter)})""".query(using PermissionRow.jdbcDecoder).selectStream()
  }
  override def selectByIdsTracked(ids: Array[PermissionId]): ZIO[ZConnection, Throwable, Map[PermissionId, PermissionRow]] = {
    selectByIds(ids).runCollect.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def selectByUniqueName(name: String): ZIO[ZConnection, Throwable, Option[PermissionRow]] = {
    sql"""select "id", "name"
          from "frontpage"."permission"
          where "name" = ${Segment.paramSegment(name)(Setter.stringSetter)}
       """.query(using PermissionRow.jdbcDecoder).selectOne
  }
  override def update: UpdateBuilder[PermissionFields, PermissionRow] = {
    UpdateBuilder(""""frontpage"."permission"""", PermissionFields.structure, PermissionRow.jdbcDecoder)
  }
  override def update(row: PermissionRow): ZIO[ZConnection, Throwable, Boolean] = {
    val id = row.id
    sql"""update "frontpage"."permission"
          set "name" = ${Segment.paramSegment(row.name)(Setter.stringSetter)}
          where "id" = ${Segment.paramSegment(id)(PermissionId.setter)}""".update.map(_ > 0)
  }
  override def upsert(unsaved: PermissionRow): ZIO[ZConnection, Throwable, UpdateResult[PermissionRow]] = {
    sql"""insert into "frontpage"."permission"("id", "name")
          values (
            ${Segment.paramSegment(unsaved.id)(PermissionId.setter)}::uuid,
            ${Segment.paramSegment(unsaved.name)(Setter.stringSetter)}
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name"
          returning "id", "name"""".insertReturning(using PermissionRow.jdbcDecoder)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: ZStream[ZConnection, Throwable, PermissionRow], batchSize: Int = 10000): ZIO[ZConnection, Throwable, Long] = {
    val created = sql"""create temporary table permission_TEMP (like "frontpage"."permission") on commit drop""".execute
    val copied = streamingInsert(s"""copy permission_TEMP("id", "name") from stdin""", batchSize, unsaved)(PermissionRow.text)
    val merged = sql"""insert into "frontpage"."permission"("id", "name")
                       select * from permission_TEMP
                       on conflict ("id")
                       do update set
                         "name" = EXCLUDED."name"
                       ;
                       drop table permission_TEMP;""".update
    created *> copied *> merged
  }
}
