/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package permission

import adventureworks.customtypes.Defaulted
import java.sql.ResultSet
import zio.jdbc.JdbcDecoder
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** Table: frontpage.permission
    Primary key: id */
case class PermissionRow(
  /** Default: gen_random_uuid() */
  id: PermissionId,
  name: String
){
   def toUnsavedRow(id: Defaulted[PermissionId]): PermissionRowUnsaved =
     PermissionRowUnsaved(name, id)
 }

object PermissionRow {
  implicit lazy val jdbcDecoder: JdbcDecoder[PermissionRow] = new JdbcDecoder[PermissionRow] {
    override def unsafeDecode(columIndex: Int, rs: ResultSet): (Int, PermissionRow) =
      columIndex + 1 ->
        PermissionRow(
          id = PermissionId.jdbcDecoder.unsafeDecode(columIndex + 0, rs)._2,
          name = JdbcDecoder.stringDecoder.unsafeDecode(columIndex + 1, rs)._2
        )
  }
  implicit lazy val jsonDecoder: JsonDecoder[PermissionRow] = JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
    val id = jsonObj.get("id").toRight("Missing field 'id'").flatMap(_.as(PermissionId.jsonDecoder))
    val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(JsonDecoder.string))
    if (id.isRight && name.isRight)
      Right(PermissionRow(id = id.toOption.get, name = name.toOption.get))
    else Left(List[Either[String, Any]](id, name).flatMap(_.left.toOption).mkString(", "))
  }
  implicit lazy val jsonEncoder: JsonEncoder[PermissionRow] = new JsonEncoder[PermissionRow] {
    override def unsafeEncode(a: PermissionRow, indent: Option[Int], out: Write): Unit = {
      out.write("{")
      out.write(""""id":""")
      PermissionId.jsonEncoder.unsafeEncode(a.id, indent, out)
      out.write(",")
      out.write(""""name":""")
      JsonEncoder.string.unsafeEncode(a.name, indent, out)
      out.write("}")
    }
  }
  implicit lazy val text: Text[PermissionRow] = Text.instance[PermissionRow]{ (row, sb) =>
    PermissionId.text.unsafeEncode(row.id, sb)
    sb.append(Text.DELIMETER)
    Text.stringInstance.unsafeEncode(row.name, sb)
  }
}
