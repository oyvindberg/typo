/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package role

import adventureworks.customtypes.Defaulted
import zio.json.JsonDecoder
import zio.json.JsonEncoder
import zio.json.ast.Json
import zio.json.internal.Write

/** This class corresponds to a row in table `frontpage.role` which has not been persisted yet */
case class RoleRowUnsaved(
  name: String,
  /** Default: gen_random_uuid() */
  id: Defaulted[RoleId] = Defaulted.UseDefault
) {
  def toRow(idDefault: => RoleId): RoleRow =
    RoleRow(
      name = name,
      id = id match {
             case Defaulted.UseDefault => idDefault
             case Defaulted.Provided(value) => value
           }
    )
}
object RoleRowUnsaved {
  implicit lazy val jsonDecoder: JsonDecoder[RoleRowUnsaved] = JsonDecoder[Json.Obj].mapOrFail { jsonObj =>
    val name = jsonObj.get("name").toRight("Missing field 'name'").flatMap(_.as(JsonDecoder.string))
    val id = jsonObj.get("id").toRight("Missing field 'id'").flatMap(_.as(Defaulted.jsonDecoder(RoleId.jsonDecoder)))
    if (name.isRight && id.isRight)
      Right(RoleRowUnsaved(name = name.toOption.get, id = id.toOption.get))
    else Left(List[Either[String, Any]](name, id).flatMap(_.left.toOption).mkString(", "))
  }
  implicit lazy val jsonEncoder: JsonEncoder[RoleRowUnsaved] = new JsonEncoder[RoleRowUnsaved] {
    override def unsafeEncode(a: RoleRowUnsaved, indent: Option[Int], out: Write): Unit = {
      out.write("{")
      out.write(""""name":""")
      JsonEncoder.string.unsafeEncode(a.name, indent, out)
      out.write(",")
      out.write(""""id":""")
      Defaulted.jsonEncoder(RoleId.jsonEncoder).unsafeEncode(a.id, indent, out)
      out.write("}")
    }
  }
  implicit lazy val text: Text[RoleRowUnsaved] = Text.instance[RoleRowUnsaved]{ (row, sb) =>
    Text.stringInstance.unsafeEncode(row.name, sb)
    sb.append(Text.DELIMETER)
    Defaulted.text(RoleId.text).unsafeEncode(row.id, sb)
  }
}
