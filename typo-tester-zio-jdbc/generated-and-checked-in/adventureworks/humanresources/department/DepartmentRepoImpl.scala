/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package humanresources
package department

import adventureworks.customtypes.Defaulted
import java.lang.Throwable
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlInterpolator
import zio.jdbc.ZConnection
import zio.stream.ZStream

object DepartmentRepoImpl extends DepartmentRepo {
  override def delete(departmentid: DepartmentId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from humanresources.department where "departmentid" = ???""".update.map(_ > 0)
  }
  override def delete: DeleteBuilder[DepartmentFields, DepartmentRow] = {
    DeleteBuilder("humanresources.department", DepartmentFields)
  }
  override def insert(unsaved: DepartmentRow): ZIO[ZConnection, Throwable, DepartmentRow] = {
    sql"""insert into humanresources.department("departmentid", "name", "groupname", "modifieddate")
          values (???::int4, ???::varchar, ???::varchar, ???::timestamp)
          returning "departmentid", "name", "groupname", "modifieddate"::text
       """.query(???).update.map(_ > 0)
  }
  override def insert(unsaved: DepartmentRowUnsaved): ZIO[ZConnection, Throwable, DepartmentRow] = {
    val fs = List(
      Some((SqlFragment(s""""name""""), sql"???::varchar")),
      Some((SqlFragment(s""""groupname""""), sql"???::varchar")),
      unsaved.departmentid match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""departmentid""""), sql"???::int4"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""modifieddate""""), sql"???::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into humanresources.department default values
            returning "departmentid", "name", "groupname", "modifieddate"::text
         """
    } else {
      import cats.syntax.foldable.toFoldableOps
      sql"""insert into humanresources.department(${fs.map { case (n, _) => n }.intercalate(sql", ")})
            values (${fs.map { case (_, f) => f }.intercalate(sql", ")})
            returning "departmentid", "name", "groupname", "modifieddate"::text
         """
    }
    q.query(???).unique
    
  }
  override def select: SelectBuilder[DepartmentFields, DepartmentRow] = {
    SelectBuilderSql("humanresources.department", DepartmentFields, DepartmentRow.read)
  }
  override def selectAll: ZStream[ZConnection, Throwable, DepartmentRow] = {
    sql"""select "departmentid", "name", "groupname", "modifieddate"::text from humanresources.department""".query[DepartmentRow].selectStream
  }
  override def selectById(departmentid: DepartmentId): ZIO[ZConnection, Throwable, Option[DepartmentRow]] = {
    sql"""select "departmentid", "name", "groupname", "modifieddate"::text from humanresources.department where "departmentid" = ???""".query[DepartmentRow].selectOne
  }
  override def selectByIds(departmentids: Array[DepartmentId]): ZStream[ZConnection, Throwable, DepartmentRow] = {
    sql"""select "departmentid", "name", "groupname", "modifieddate"::text from humanresources.department where "departmentid" = ANY(${departmentids})""".query[DepartmentRow].selectStream
  }
  override def update(row: DepartmentRow): ZIO[ZConnection, Throwable, Boolean] = {
    val departmentid = row.departmentid
    sql"""update humanresources.department
          set "name" = ???::varchar,
              "groupname" = ???::varchar,
              "modifieddate" = ???::timestamp
          where "departmentid" = ???""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[DepartmentFields, DepartmentRow] = {
    UpdateBuilder("humanresources.department", DepartmentFields, DepartmentRow.read)
  }
  override def upsert(unsaved: DepartmentRow): ZIO[ZConnection, Throwable, DepartmentRow] = {
    sql"""insert into humanresources.department("departmentid", "name", "groupname", "modifieddate")
          values (
            ???::int4,
            ???::varchar,
            ???::varchar,
            ???::timestamp
          )
          on conflict ("departmentid")
          do update set
            "name" = EXCLUDED."name",
            "groupname" = EXCLUDED."groupname",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "departmentid", "name", "groupname", "modifieddate"::text
       """.query(???).unique
  }
}
