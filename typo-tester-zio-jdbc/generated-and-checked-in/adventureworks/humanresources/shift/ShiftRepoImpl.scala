/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package humanresources
package shift

import adventureworks.customtypes.Defaulted
import java.lang.Throwable
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlInterpolator
import zio.jdbc.ZConnection
import zio.stream.ZStream

object ShiftRepoImpl extends ShiftRepo {
  override def delete(shiftid: ShiftId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from humanresources.shift where "shiftid" = ???""".update.map(_ > 0)
  }
  override def delete: DeleteBuilder[ShiftFields, ShiftRow] = {
    DeleteBuilder("humanresources.shift", ShiftFields)
  }
  override def insert(unsaved: ShiftRow): ZIO[ZConnection, Throwable, ShiftRow] = {
    sql"""insert into humanresources.shift("shiftid", "name", "starttime", "endtime", "modifieddate")
          values (???::int4, ???::varchar, ???::time, ???::time, ???::timestamp)
          returning "shiftid", "name", "starttime"::text, "endtime"::text, "modifieddate"::text
       """.query(???).update.map(_ > 0)
  }
  override def insert(unsaved: ShiftRowUnsaved): ZIO[ZConnection, Throwable, ShiftRow] = {
    val fs = List(
      Some((SqlFragment(s""""name""""), sql"???::varchar")),
      Some((SqlFragment(s""""starttime""""), sql"???::time")),
      Some((SqlFragment(s""""endtime""""), sql"???::time")),
      unsaved.shiftid match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""shiftid""""), sql"???::int4"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""modifieddate""""), sql"???::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into humanresources.shift default values
            returning "shiftid", "name", "starttime"::text, "endtime"::text, "modifieddate"::text
         """
    } else {
      import cats.syntax.foldable.toFoldableOps
      sql"""insert into humanresources.shift(${fs.map { case (n, _) => n }.intercalate(sql", ")})
            values (${fs.map { case (_, f) => f }.intercalate(sql", ")})
            returning "shiftid", "name", "starttime"::text, "endtime"::text, "modifieddate"::text
         """
    }
    q.query(???).unique
    
  }
  override def select: SelectBuilder[ShiftFields, ShiftRow] = {
    SelectBuilderSql("humanresources.shift", ShiftFields, ShiftRow.read)
  }
  override def selectAll: ZStream[ZConnection, Throwable, ShiftRow] = {
    sql"""select "shiftid", "name", "starttime"::text, "endtime"::text, "modifieddate"::text from humanresources.shift""".query[ShiftRow].selectStream
  }
  override def selectById(shiftid: ShiftId): ZIO[ZConnection, Throwable, Option[ShiftRow]] = {
    sql"""select "shiftid", "name", "starttime"::text, "endtime"::text, "modifieddate"::text from humanresources.shift where "shiftid" = ???""".query[ShiftRow].selectOne
  }
  override def selectByIds(shiftids: Array[ShiftId]): ZStream[ZConnection, Throwable, ShiftRow] = {
    sql"""select "shiftid", "name", "starttime"::text, "endtime"::text, "modifieddate"::text from humanresources.shift where "shiftid" = ANY(${shiftids})""".query[ShiftRow].selectStream
  }
  override def update(row: ShiftRow): ZIO[ZConnection, Throwable, Boolean] = {
    val shiftid = row.shiftid
    sql"""update humanresources.shift
          set "name" = ???::varchar,
              "starttime" = ???::time,
              "endtime" = ???::time,
              "modifieddate" = ???::timestamp
          where "shiftid" = ???""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[ShiftFields, ShiftRow] = {
    UpdateBuilder("humanresources.shift", ShiftFields, ShiftRow.read)
  }
  override def upsert(unsaved: ShiftRow): ZIO[ZConnection, Throwable, ShiftRow] = {
    sql"""insert into humanresources.shift("shiftid", "name", "starttime", "endtime", "modifieddate")
          values (
            ???::int4,
            ???::varchar,
            ???::time,
            ???::time,
            ???::timestamp
          )
          on conflict ("shiftid")
          do update set
            "name" = EXCLUDED."name",
            "starttime" = EXCLUDED."starttime",
            "endtime" = EXCLUDED."endtime",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "shiftid", "name", "starttime"::text, "endtime"::text, "modifieddate"::text
       """.query(???).unique
  }
}
