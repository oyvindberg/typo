/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package production
package productreview

import adventureworks.customtypes.Defaulted
import java.lang.Throwable
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlInterpolator
import zio.jdbc.ZConnection
import zio.stream.ZStream

object ProductreviewRepoImpl extends ProductreviewRepo {
  override def delete(productreviewid: ProductreviewId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from production.productreview where "productreviewid" = ???""".update.map(_ > 0)
  }
  override def delete: DeleteBuilder[ProductreviewFields, ProductreviewRow] = {
    DeleteBuilder("production.productreview", ProductreviewFields)
  }
  override def insert(unsaved: ProductreviewRow): ZIO[ZConnection, Throwable, ProductreviewRow] = {
    sql"""insert into production.productreview("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
          values (???::int4, ???::int4, ???::varchar, ???::timestamp, ???, ???::int4, ???, ???::timestamp)
          returning "productreviewid", "productid", "reviewername", "reviewdate"::text, "emailaddress", "rating", "comments", "modifieddate"::text
       """.query(???).update.map(_ > 0)
  }
  override def insert(unsaved: ProductreviewRowUnsaved): ZIO[ZConnection, Throwable, ProductreviewRow] = {
    val fs = List(
      Some((SqlFragment(s""""productid""""), sql"???::int4")),
      Some((SqlFragment(s""""reviewername""""), sql"???::varchar")),
      Some((SqlFragment(s""""emailaddress""""), sql"???")),
      Some((SqlFragment(s""""rating""""), sql"???::int4")),
      Some((SqlFragment(s""""comments""""), sql"???")),
      unsaved.productreviewid match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""productreviewid""""), sql"???::int4"))
      },
      unsaved.reviewdate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""reviewdate""""), sql"???::timestamp"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""modifieddate""""), sql"???::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into production.productreview default values
            returning "productreviewid", "productid", "reviewername", "reviewdate"::text, "emailaddress", "rating", "comments", "modifieddate"::text
         """
    } else {
      import cats.syntax.foldable.toFoldableOps
      sql"""insert into production.productreview(${fs.map { case (n, _) => n }.intercalate(sql", ")})
            values (${fs.map { case (_, f) => f }.intercalate(sql", ")})
            returning "productreviewid", "productid", "reviewername", "reviewdate"::text, "emailaddress", "rating", "comments", "modifieddate"::text
         """
    }
    q.query(???).unique
    
  }
  override def select: SelectBuilder[ProductreviewFields, ProductreviewRow] = {
    SelectBuilderSql("production.productreview", ProductreviewFields, ProductreviewRow.read)
  }
  override def selectAll: ZStream[ZConnection, Throwable, ProductreviewRow] = {
    sql"""select "productreviewid", "productid", "reviewername", "reviewdate"::text, "emailaddress", "rating", "comments", "modifieddate"::text from production.productreview""".query[ProductreviewRow].selectStream
  }
  override def selectById(productreviewid: ProductreviewId): ZIO[ZConnection, Throwable, Option[ProductreviewRow]] = {
    sql"""select "productreviewid", "productid", "reviewername", "reviewdate"::text, "emailaddress", "rating", "comments", "modifieddate"::text from production.productreview where "productreviewid" = ???""".query[ProductreviewRow].selectOne
  }
  override def selectByIds(productreviewids: Array[ProductreviewId]): ZStream[ZConnection, Throwable, ProductreviewRow] = {
    sql"""select "productreviewid", "productid", "reviewername", "reviewdate"::text, "emailaddress", "rating", "comments", "modifieddate"::text from production.productreview where "productreviewid" = ANY(${productreviewids})""".query[ProductreviewRow].selectStream
  }
  override def update(row: ProductreviewRow): ZIO[ZConnection, Throwable, Boolean] = {
    val productreviewid = row.productreviewid
    sql"""update production.productreview
          set "productid" = ???::int4,
              "reviewername" = ???::varchar,
              "reviewdate" = ???::timestamp,
              "emailaddress" = ???,
              "rating" = ???::int4,
              "comments" = ???,
              "modifieddate" = ???::timestamp
          where "productreviewid" = ???""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[ProductreviewFields, ProductreviewRow] = {
    UpdateBuilder("production.productreview", ProductreviewFields, ProductreviewRow.read)
  }
  override def upsert(unsaved: ProductreviewRow): ZIO[ZConnection, Throwable, ProductreviewRow] = {
    sql"""insert into production.productreview("productreviewid", "productid", "reviewername", "reviewdate", "emailaddress", "rating", "comments", "modifieddate")
          values (
            ???::int4,
            ???::int4,
            ???::varchar,
            ???::timestamp,
            ???,
            ???::int4,
            ???,
            ???::timestamp
          )
          on conflict ("productreviewid")
          do update set
            "productid" = EXCLUDED."productid",
            "reviewername" = EXCLUDED."reviewername",
            "reviewdate" = EXCLUDED."reviewdate",
            "emailaddress" = EXCLUDED."emailaddress",
            "rating" = EXCLUDED."rating",
            "comments" = EXCLUDED."comments",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "productreviewid", "productid", "reviewername", "reviewdate"::text, "emailaddress", "rating", "comments", "modifieddate"::text
       """.query(???).unique
  }
}
