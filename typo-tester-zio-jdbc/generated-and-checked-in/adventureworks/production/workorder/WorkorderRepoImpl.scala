/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package production
package workorder

import adventureworks.customtypes.Defaulted
import java.lang.Throwable
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlInterpolator
import zio.jdbc.ZConnection
import zio.stream.ZStream

object WorkorderRepoImpl extends WorkorderRepo {
  override def delete(workorderid: WorkorderId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from production.workorder where "workorderid" = ???""".update.map(_ > 0)
  }
  override def delete: DeleteBuilder[WorkorderFields, WorkorderRow] = {
    DeleteBuilder("production.workorder", WorkorderFields)
  }
  override def insert(unsaved: WorkorderRow): ZIO[ZConnection, Throwable, WorkorderRow] = {
    sql"""insert into production.workorder("workorderid", "productid", "orderqty", "scrappedqty", "startdate", "enddate", "duedate", "scrapreasonid", "modifieddate")
          values (???::int4, ???::int4, ???::int4, ???::int2, ???::timestamp, ???::timestamp, ???::timestamp, ???::int2, ???::timestamp)
          returning "workorderid", "productid", "orderqty", "scrappedqty", "startdate"::text, "enddate"::text, "duedate"::text, "scrapreasonid", "modifieddate"::text
       """.query(???).update.map(_ > 0)
  }
  override def insert(unsaved: WorkorderRowUnsaved): ZIO[ZConnection, Throwable, WorkorderRow] = {
    val fs = List(
      Some((SqlFragment(s""""productid""""), sql"???::int4")),
      Some((SqlFragment(s""""orderqty""""), sql"???::int4")),
      Some((SqlFragment(s""""scrappedqty""""), sql"???::int2")),
      Some((SqlFragment(s""""startdate""""), sql"???::timestamp")),
      Some((SqlFragment(s""""enddate""""), sql"???::timestamp")),
      Some((SqlFragment(s""""duedate""""), sql"???::timestamp")),
      Some((SqlFragment(s""""scrapreasonid""""), sql"???::int2")),
      unsaved.workorderid match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""workorderid""""), sql"???::int4"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""modifieddate""""), sql"???::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into production.workorder default values
            returning "workorderid", "productid", "orderqty", "scrappedqty", "startdate"::text, "enddate"::text, "duedate"::text, "scrapreasonid", "modifieddate"::text
         """
    } else {
      import cats.syntax.foldable.toFoldableOps
      sql"""insert into production.workorder(${fs.map { case (n, _) => n }.intercalate(sql", ")})
            values (${fs.map { case (_, f) => f }.intercalate(sql", ")})
            returning "workorderid", "productid", "orderqty", "scrappedqty", "startdate"::text, "enddate"::text, "duedate"::text, "scrapreasonid", "modifieddate"::text
         """
    }
    q.query(???).unique
    
  }
  override def select: SelectBuilder[WorkorderFields, WorkorderRow] = {
    SelectBuilderSql("production.workorder", WorkorderFields, WorkorderRow.read)
  }
  override def selectAll: ZStream[ZConnection, Throwable, WorkorderRow] = {
    sql"""select "workorderid", "productid", "orderqty", "scrappedqty", "startdate"::text, "enddate"::text, "duedate"::text, "scrapreasonid", "modifieddate"::text from production.workorder""".query[WorkorderRow].selectStream
  }
  override def selectById(workorderid: WorkorderId): ZIO[ZConnection, Throwable, Option[WorkorderRow]] = {
    sql"""select "workorderid", "productid", "orderqty", "scrappedqty", "startdate"::text, "enddate"::text, "duedate"::text, "scrapreasonid", "modifieddate"::text from production.workorder where "workorderid" = ???""".query[WorkorderRow].selectOne
  }
  override def selectByIds(workorderids: Array[WorkorderId]): ZStream[ZConnection, Throwable, WorkorderRow] = {
    sql"""select "workorderid", "productid", "orderqty", "scrappedqty", "startdate"::text, "enddate"::text, "duedate"::text, "scrapreasonid", "modifieddate"::text from production.workorder where "workorderid" = ANY(${workorderids})""".query[WorkorderRow].selectStream
  }
  override def update(row: WorkorderRow): ZIO[ZConnection, Throwable, Boolean] = {
    val workorderid = row.workorderid
    sql"""update production.workorder
          set "productid" = ???::int4,
              "orderqty" = ???::int4,
              "scrappedqty" = ???::int2,
              "startdate" = ???::timestamp,
              "enddate" = ???::timestamp,
              "duedate" = ???::timestamp,
              "scrapreasonid" = ???::int2,
              "modifieddate" = ???::timestamp
          where "workorderid" = ???""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[WorkorderFields, WorkorderRow] = {
    UpdateBuilder("production.workorder", WorkorderFields, WorkorderRow.read)
  }
  override def upsert(unsaved: WorkorderRow): ZIO[ZConnection, Throwable, WorkorderRow] = {
    sql"""insert into production.workorder("workorderid", "productid", "orderqty", "scrappedqty", "startdate", "enddate", "duedate", "scrapreasonid", "modifieddate")
          values (
            ???::int4,
            ???::int4,
            ???::int4,
            ???::int2,
            ???::timestamp,
            ???::timestamp,
            ???::timestamp,
            ???::int2,
            ???::timestamp
          )
          on conflict ("workorderid")
          do update set
            "productid" = EXCLUDED."productid",
            "orderqty" = EXCLUDED."orderqty",
            "scrappedqty" = EXCLUDED."scrappedqty",
            "startdate" = EXCLUDED."startdate",
            "enddate" = EXCLUDED."enddate",
            "duedate" = EXCLUDED."duedate",
            "scrapreasonid" = EXCLUDED."scrapreasonid",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "workorderid", "productid", "orderqty", "scrappedqty", "startdate"::text, "enddate"::text, "duedate"::text, "scrapreasonid", "modifieddate"::text
       """.query(???).unique
  }
}
