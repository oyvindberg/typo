/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package production
package productcosthistory

import adventureworks.customtypes.Defaulted
import java.lang.Throwable
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlInterpolator
import zio.jdbc.ZConnection
import zio.stream.ZStream

object ProductcosthistoryRepoImpl extends ProductcosthistoryRepo {
  override def delete(compositeId: ProductcosthistoryId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from production.productcosthistory where "productid" = ??? AND "startdate" = ???""".update.map(_ > 0)
  }
  override def delete: DeleteBuilder[ProductcosthistoryFields, ProductcosthistoryRow] = {
    DeleteBuilder("production.productcosthistory", ProductcosthistoryFields)
  }
  override def insert(unsaved: ProductcosthistoryRow): ZIO[ZConnection, Throwable, ProductcosthistoryRow] = {
    sql"""insert into production.productcosthistory("productid", "startdate", "enddate", "standardcost", "modifieddate")
          values (???::int4, ???::timestamp, ???::timestamp, ???::numeric, ???::timestamp)
          returning "productid", "startdate"::text, "enddate"::text, "standardcost", "modifieddate"::text
       """.query(???).update.map(_ > 0)
  }
  override def insert(unsaved: ProductcosthistoryRowUnsaved): ZIO[ZConnection, Throwable, ProductcosthistoryRow] = {
    val fs = List(
      Some((SqlFragment(s""""productid""""), sql"???::int4")),
      Some((SqlFragment(s""""startdate""""), sql"???::timestamp")),
      Some((SqlFragment(s""""enddate""""), sql"???::timestamp")),
      Some((SqlFragment(s""""standardcost""""), sql"???::numeric")),
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""modifieddate""""), sql"???::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into production.productcosthistory default values
            returning "productid", "startdate"::text, "enddate"::text, "standardcost", "modifieddate"::text
         """
    } else {
      import cats.syntax.foldable.toFoldableOps
      sql"""insert into production.productcosthistory(${fs.map { case (n, _) => n }.intercalate(sql", ")})
            values (${fs.map { case (_, f) => f }.intercalate(sql", ")})
            returning "productid", "startdate"::text, "enddate"::text, "standardcost", "modifieddate"::text
         """
    }
    q.query(???).unique
    
  }
  override def select: SelectBuilder[ProductcosthistoryFields, ProductcosthistoryRow] = {
    SelectBuilderSql("production.productcosthistory", ProductcosthistoryFields, ProductcosthistoryRow.read)
  }
  override def selectAll: ZStream[ZConnection, Throwable, ProductcosthistoryRow] = {
    sql"""select "productid", "startdate"::text, "enddate"::text, "standardcost", "modifieddate"::text from production.productcosthistory""".query[ProductcosthistoryRow].selectStream
  }
  override def selectById(compositeId: ProductcosthistoryId): ZIO[ZConnection, Throwable, Option[ProductcosthistoryRow]] = {
    sql"""select "productid", "startdate"::text, "enddate"::text, "standardcost", "modifieddate"::text from production.productcosthistory where "productid" = ??? AND "startdate" = ???""".query[ProductcosthistoryRow].selectOne
  }
  override def update(row: ProductcosthistoryRow): ZIO[ZConnection, Throwable, Boolean] = {
    val compositeId = row.compositeId
    sql"""update production.productcosthistory
          set "enddate" = ???::timestamp,
              "standardcost" = ???::numeric,
              "modifieddate" = ???::timestamp
          where "productid" = ??? AND "startdate" = ???""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[ProductcosthistoryFields, ProductcosthistoryRow] = {
    UpdateBuilder("production.productcosthistory", ProductcosthistoryFields, ProductcosthistoryRow.read)
  }
  override def upsert(unsaved: ProductcosthistoryRow): ZIO[ZConnection, Throwable, ProductcosthistoryRow] = {
    sql"""insert into production.productcosthistory("productid", "startdate", "enddate", "standardcost", "modifieddate")
          values (
            ???::int4,
            ???::timestamp,
            ???::timestamp,
            ???::numeric,
            ???::timestamp
          )
          on conflict ("productid", "startdate")
          do update set
            "enddate" = EXCLUDED."enddate",
            "standardcost" = EXCLUDED."standardcost",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "productid", "startdate"::text, "enddate"::text, "standardcost", "modifieddate"::text
       """.query(???).unique
  }
}
