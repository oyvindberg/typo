/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package sales
package currency

import adventureworks.customtypes.Defaulted
import java.lang.Throwable
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlInterpolator
import zio.jdbc.ZConnection
import zio.stream.ZStream

object CurrencyRepoImpl extends CurrencyRepo {
  override def delete(currencycode: CurrencyId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from sales.currency where "currencycode" = ???""".update.map(_ > 0)
  }
  override def delete: DeleteBuilder[CurrencyFields, CurrencyRow] = {
    DeleteBuilder("sales.currency", CurrencyFields)
  }
  override def insert(unsaved: CurrencyRow): ZIO[ZConnection, Throwable, CurrencyRow] = {
    sql"""insert into sales.currency("currencycode", "name", "modifieddate")
          values (???::bpchar, ???::varchar, ???::timestamp)
          returning "currencycode", "name", "modifieddate"::text
       """.query(???).update.map(_ > 0)
  }
  override def insert(unsaved: CurrencyRowUnsaved): ZIO[ZConnection, Throwable, CurrencyRow] = {
    val fs = List(
      Some((SqlFragment(s""""currencycode""""), sql"???::bpchar")),
      Some((SqlFragment(s""""name""""), sql"???::varchar")),
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""modifieddate""""), sql"???::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into sales.currency default values
            returning "currencycode", "name", "modifieddate"::text
         """
    } else {
      import cats.syntax.foldable.toFoldableOps
      sql"""insert into sales.currency(${fs.map { case (n, _) => n }.intercalate(sql", ")})
            values (${fs.map { case (_, f) => f }.intercalate(sql", ")})
            returning "currencycode", "name", "modifieddate"::text
         """
    }
    q.query(???).unique
    
  }
  override def select: SelectBuilder[CurrencyFields, CurrencyRow] = {
    SelectBuilderSql("sales.currency", CurrencyFields, CurrencyRow.read)
  }
  override def selectAll: ZStream[ZConnection, Throwable, CurrencyRow] = {
    sql"""select "currencycode", "name", "modifieddate"::text from sales.currency""".query[CurrencyRow].selectStream
  }
  override def selectById(currencycode: CurrencyId): ZIO[ZConnection, Throwable, Option[CurrencyRow]] = {
    sql"""select "currencycode", "name", "modifieddate"::text from sales.currency where "currencycode" = ???""".query[CurrencyRow].selectOne
  }
  override def selectByIds(currencycodes: Array[CurrencyId]): ZStream[ZConnection, Throwable, CurrencyRow] = {
    sql"""select "currencycode", "name", "modifieddate"::text from sales.currency where "currencycode" = ANY(${currencycodes})""".query[CurrencyRow].selectStream
  }
  override def update(row: CurrencyRow): ZIO[ZConnection, Throwable, Boolean] = {
    val currencycode = row.currencycode
    sql"""update sales.currency
          set "name" = ???::varchar,
              "modifieddate" = ???::timestamp
          where "currencycode" = ???""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[CurrencyFields, CurrencyRow] = {
    UpdateBuilder("sales.currency", CurrencyFields, CurrencyRow.read)
  }
  override def upsert(unsaved: CurrencyRow): ZIO[ZConnection, Throwable, CurrencyRow] = {
    sql"""insert into sales.currency("currencycode", "name", "modifieddate")
          values (
            ???::bpchar,
            ???::varchar,
            ???::timestamp
          )
          on conflict ("currencycode")
          do update set
            "name" = EXCLUDED."name",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "currencycode", "name", "modifieddate"::text
       """.query(???).unique
  }
}
