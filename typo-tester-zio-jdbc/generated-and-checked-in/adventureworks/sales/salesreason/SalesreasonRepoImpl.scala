/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package sales
package salesreason

import adventureworks.customtypes.Defaulted
import java.lang.Throwable
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder
import zio.ZIO
import zio.jdbc.SqlFragment
import zio.jdbc.SqlInterpolator
import zio.jdbc.ZConnection
import zio.stream.ZStream

object SalesreasonRepoImpl extends SalesreasonRepo {
  override def delete(salesreasonid: SalesreasonId): ZIO[ZConnection, Throwable, Boolean] = {
    sql"""delete from sales.salesreason where "salesreasonid" = ???""".update.map(_ > 0)
  }
  override def delete: DeleteBuilder[SalesreasonFields, SalesreasonRow] = {
    DeleteBuilder("sales.salesreason", SalesreasonFields)
  }
  override def insert(unsaved: SalesreasonRow): ZIO[ZConnection, Throwable, SalesreasonRow] = {
    sql"""insert into sales.salesreason("salesreasonid", "name", "reasontype", "modifieddate")
          values (???::int4, ???::varchar, ???::varchar, ???::timestamp)
          returning "salesreasonid", "name", "reasontype", "modifieddate"::text
       """.query(???).update.map(_ > 0)
  }
  override def insert(unsaved: SalesreasonRowUnsaved): ZIO[ZConnection, Throwable, SalesreasonRow] = {
    val fs = List(
      Some((SqlFragment(s""""name""""), sql"???::varchar")),
      Some((SqlFragment(s""""reasontype""""), sql"???::varchar")),
      unsaved.salesreasonid match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""salesreasonid""""), sql"???::int4"))
      },
      unsaved.modifieddate match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((SqlFragment(s""""modifieddate""""), sql"???::timestamp"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into sales.salesreason default values
            returning "salesreasonid", "name", "reasontype", "modifieddate"::text
         """
    } else {
      import cats.syntax.foldable.toFoldableOps
      sql"""insert into sales.salesreason(${fs.map { case (n, _) => n }.intercalate(sql", ")})
            values (${fs.map { case (_, f) => f }.intercalate(sql", ")})
            returning "salesreasonid", "name", "reasontype", "modifieddate"::text
         """
    }
    q.query(???).unique
    
  }
  override def select: SelectBuilder[SalesreasonFields, SalesreasonRow] = {
    SelectBuilderSql("sales.salesreason", SalesreasonFields, SalesreasonRow.read)
  }
  override def selectAll: ZStream[ZConnection, Throwable, SalesreasonRow] = {
    sql"""select "salesreasonid", "name", "reasontype", "modifieddate"::text from sales.salesreason""".query[SalesreasonRow].selectStream
  }
  override def selectById(salesreasonid: SalesreasonId): ZIO[ZConnection, Throwable, Option[SalesreasonRow]] = {
    sql"""select "salesreasonid", "name", "reasontype", "modifieddate"::text from sales.salesreason where "salesreasonid" = ???""".query[SalesreasonRow].selectOne
  }
  override def selectByIds(salesreasonids: Array[SalesreasonId]): ZStream[ZConnection, Throwable, SalesreasonRow] = {
    sql"""select "salesreasonid", "name", "reasontype", "modifieddate"::text from sales.salesreason where "salesreasonid" = ANY(${salesreasonids})""".query[SalesreasonRow].selectStream
  }
  override def update(row: SalesreasonRow): ZIO[ZConnection, Throwable, Boolean] = {
    val salesreasonid = row.salesreasonid
    sql"""update sales.salesreason
          set "name" = ???::varchar,
              "reasontype" = ???::varchar,
              "modifieddate" = ???::timestamp
          where "salesreasonid" = ???""".update.map(_ > 0)
  }
  override def update: UpdateBuilder[SalesreasonFields, SalesreasonRow] = {
    UpdateBuilder("sales.salesreason", SalesreasonFields, SalesreasonRow.read)
  }
  override def upsert(unsaved: SalesreasonRow): ZIO[ZConnection, Throwable, SalesreasonRow] = {
    sql"""insert into sales.salesreason("salesreasonid", "name", "reasontype", "modifieddate")
          values (
            ???::int4,
            ???::varchar,
            ???::varchar,
            ???::timestamp
          )
          on conflict ("salesreasonid")
          do update set
            "name" = EXCLUDED."name",
            "reasontype" = EXCLUDED."reasontype",
            "modifieddate" = EXCLUDED."modifieddate"
          returning "salesreasonid", "name", "reasontype", "modifieddate"::text
       """.query(???).unique
  }
}
