/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pe
package sp

import adventureworks.person.countryregion.CountryregionId
import adventureworks.person.stateprovince.StateprovinceId
import adventureworks.public.Flag
import adventureworks.public.Name
import adventureworks.sales.salesterritory.SalesterritoryId
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime
import java.util.UUID

object SpRepoImpl extends SpRepo {
  override def selectAll(implicit c: Connection): List[SpRow] = {
    SQL"""select "id", stateprovinceid, stateprovincecode, countryregioncode, isonlystateprovinceflag, "name", territoryid, rowguid, modifieddate
          from pe.sp
       """.as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[SpFieldOrIdValue[_]])(implicit c: Connection): List[SpRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case SpFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case SpFieldValue.stateprovinceid(value) => NamedParameter("stateprovinceid", ParameterValue.from(value))
          case SpFieldValue.stateprovincecode(value) => NamedParameter("stateprovincecode", ParameterValue.from(value))
          case SpFieldValue.countryregioncode(value) => NamedParameter("countryregioncode", ParameterValue.from(value))
          case SpFieldValue.isonlystateprovinceflag(value) => NamedParameter("isonlystateprovinceflag", ParameterValue.from(value))
          case SpFieldValue.name(value) => NamedParameter("name", ParameterValue.from(value))
          case SpFieldValue.territoryid(value) => NamedParameter("territoryid", ParameterValue.from(value))
          case SpFieldValue.rowguid(value) => NamedParameter("rowguid", ParameterValue.from(value))
          case SpFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select "id", stateprovinceid, stateprovincecode, countryregioncode, isonlystateprovinceflag, "name", territoryid, rowguid, modifieddate
                    from pe.sp
                    where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}
                 """
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[SpRow] =
    RowParser[SpRow] { row =>
      Success(
        SpRow(
          id = row[Option[Int]]("id"),
          stateprovinceid = row[Option[StateprovinceId]]("stateprovinceid"),
          stateprovincecode = row[Option[/* bpchar */ String]]("stateprovincecode"),
          countryregioncode = row[Option[CountryregionId]]("countryregioncode"),
          isonlystateprovinceflag = row[Flag]("isonlystateprovinceflag"),
          name = row[Option[Name]]("name"),
          territoryid = row[Option[SalesterritoryId]]("territoryid"),
          rowguid = row[Option[UUID]]("rowguid"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
