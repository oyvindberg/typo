/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pe
package e

import adventureworks.person.businessentity.BusinessentityId
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime
import java.util.UUID

object ERepoImpl extends ERepo {
  override def selectAll(implicit c: Connection): List[ERow] = {
    SQL"""select "id", businessentityid, emailaddressid, emailaddress, rowguid, modifieddate
          from pe.e
       """.as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[EFieldOrIdValue[_]])(implicit c: Connection): List[ERow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case EFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case EFieldValue.businessentityid(value) => NamedParameter("businessentityid", ParameterValue.from(value))
          case EFieldValue.emailaddressid(value) => NamedParameter("emailaddressid", ParameterValue.from(value))
          case EFieldValue.emailaddress(value) => NamedParameter("emailaddress", ParameterValue.from(value))
          case EFieldValue.rowguid(value) => NamedParameter("rowguid", ParameterValue.from(value))
          case EFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select "id", businessentityid, emailaddressid, emailaddress, rowguid, modifieddate
                    from pe.e
                    where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}
                 """
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[ERow] =
    RowParser[ERow] { row =>
      Success(
        ERow(
          id = row[Option[Int]]("id"),
          businessentityid = row[Option[BusinessentityId]]("businessentityid"),
          emailaddressid = row[Option[Int]]("emailaddressid"),
          emailaddress = row[Option[String]]("emailaddress"),
          rowguid = row[Option[UUID]]("rowguid"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
