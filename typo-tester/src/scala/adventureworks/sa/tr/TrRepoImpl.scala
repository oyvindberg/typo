/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package sa
package tr

import adventureworks.person.stateprovince.StateprovinceId
import adventureworks.public.Name
import adventureworks.sales.salestaxrate.SalestaxrateId
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime
import java.util.UUID

object TrRepoImpl extends TrRepo {
  override def selectAll(implicit c: Connection): List[TrRow] = {
    SQL"""select "id", salestaxrateid, stateprovinceid, taxtype, taxrate, "name", rowguid, modifieddate
          from sa.tr
       """.as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[TrFieldOrIdValue[_]])(implicit c: Connection): List[TrRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case TrFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case TrFieldValue.salestaxrateid(value) => NamedParameter("salestaxrateid", ParameterValue.from(value))
          case TrFieldValue.stateprovinceid(value) => NamedParameter("stateprovinceid", ParameterValue.from(value))
          case TrFieldValue.taxtype(value) => NamedParameter("taxtype", ParameterValue.from(value))
          case TrFieldValue.taxrate(value) => NamedParameter("taxrate", ParameterValue.from(value))
          case TrFieldValue.name(value) => NamedParameter("name", ParameterValue.from(value))
          case TrFieldValue.rowguid(value) => NamedParameter("rowguid", ParameterValue.from(value))
          case TrFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val quote = '"'.toString
        val q = s"""select "id", salestaxrateid, stateprovinceid, taxtype, taxrate, "name", rowguid, modifieddate
                    from sa.tr
                    where ${namedParams.map(x => s"$quote${x.name}$quote = {${x.name}}").mkString(" AND ")}
                 """
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[TrRow] =
    RowParser[TrRow] { row =>
      Success(
        TrRow(
          id = row[Option[Int]]("id"),
          salestaxrateid = row[Option[SalestaxrateId]]("salestaxrateid"),
          stateprovinceid = row[Option[StateprovinceId]]("stateprovinceid"),
          taxtype = row[Option[Int]]("taxtype"),
          taxrate = row[Option[BigDecimal]]("taxrate"),
          name = row[Option[Name]]("name"),
          rowguid = row[Option[UUID]]("rowguid"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
