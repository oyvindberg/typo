/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package sa
package s

import adventureworks.person.businessentity.BusinessentityId
import adventureworks.public.Name
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime
import java.util.UUID

object SRepoImpl extends SRepo {
  override def selectAll(implicit c: Connection): List[SRow] = {
    SQL"""select "id", businessentityid, "name", salespersonid, demographics, rowguid, modifieddate
          from sa.s
       """.as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[SFieldOrIdValue[_]])(implicit c: Connection): List[SRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case SFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case SFieldValue.businessentityid(value) => NamedParameter("businessentityid", ParameterValue.from(value))
          case SFieldValue.name(value) => NamedParameter("name", ParameterValue.from(value))
          case SFieldValue.salespersonid(value) => NamedParameter("salespersonid", ParameterValue.from(value))
          case SFieldValue.demographics(value) => NamedParameter("demographics", ParameterValue.from(value))
          case SFieldValue.rowguid(value) => NamedParameter("rowguid", ParameterValue.from(value))
          case SFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select "id", businessentityid, "name", salespersonid, demographics, rowguid, modifieddate
                    from sa.s
                    where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}
                 """
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[SRow] =
    RowParser[SRow] { row =>
      Success(
        SRow(
          id = row[Option[Int]]("id"),
          businessentityid = row[Option[BusinessentityId]]("businessentityid"),
          name = row[Option[Name]]("name"),
          salespersonid = row[Option[BusinessentityId]]("salespersonid"),
          demographics = row[Option[/* xml */ String]]("demographics"),
          rowguid = row[Option[UUID]]("rowguid"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
