/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pr
package um

import adventureworks.production.unitmeasure.UnitmeasureId
import adventureworks.public.Name
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime

object UmRepoImpl extends UmRepo {
  override def selectAll(implicit c: Connection): List[UmRow] = {
    SQL"""select "id", unitmeasurecode, "name", modifieddate
          from pr.um
       """.as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[UmFieldOrIdValue[_]])(implicit c: Connection): List[UmRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case UmFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case UmFieldValue.unitmeasurecode(value) => NamedParameter("unitmeasurecode", ParameterValue.from(value))
          case UmFieldValue.name(value) => NamedParameter("name", ParameterValue.from(value))
          case UmFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val quote = '"'.toString
        val q = s"""select "id", unitmeasurecode, "name", modifieddate
                    from pr.um
                    where ${namedParams.map(x => s"$quote${x.name}$quote = {${x.name}}").mkString(" AND ")}
                 """
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[UmRow] =
    RowParser[UmRow] { row =>
      Success(
        UmRow(
          id = row[Option[/* bpchar */ String]]("id"),
          unitmeasurecode = row[Option[UnitmeasureId]]("unitmeasurecode"),
          name = row[Option[Name]]("name"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
