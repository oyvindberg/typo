/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package sa
package sci

import adventureworks.production.product.ProductId
import adventureworks.sales.shoppingcartitem.ShoppingcartitemId
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime

object SciRepoImpl extends SciRepo {
  override def selectAll(implicit c: Connection): List[SciRow] = {
    SQL"""select id, shoppingcartitemid, shoppingcartid, quantity, productid, datecreated, modifieddate
          from sa.sci
       """.as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[SciFieldOrIdValue[_]])(implicit c: Connection): List[SciRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case SciFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case SciFieldValue.shoppingcartitemid(value) => NamedParameter("shoppingcartitemid", ParameterValue.from(value))
          case SciFieldValue.shoppingcartid(value) => NamedParameter("shoppingcartid", ParameterValue.from(value))
          case SciFieldValue.quantity(value) => NamedParameter("quantity", ParameterValue.from(value))
          case SciFieldValue.productid(value) => NamedParameter("productid", ParameterValue.from(value))
          case SciFieldValue.datecreated(value) => NamedParameter("datecreated", ParameterValue.from(value))
          case SciFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select *
                    from sa.sci
                    where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}
                 """
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[SciRow] =
    RowParser[SciRow] { row =>
      Success(
        SciRow(
          id = row[Option[Int]]("id"),
          shoppingcartitemid = row[Option[ShoppingcartitemId]]("shoppingcartitemid"),
          shoppingcartid = row[Option[String]]("shoppingcartid"),
          quantity = row[Option[Int]]("quantity"),
          productid = row[Option[ProductId]]("productid"),
          datecreated = row[Option[LocalDateTime]]("datecreated"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
