/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pr
package pi

import anorm.NamedParameter
import anorm.ParameterValue
import anorm.SqlStringInterpolation
import java.sql.Connection

object PiRepoImpl extends PiRepo {
  override def selectAll(implicit c: Connection): List[PiRow] = {
    SQL"""select id, productid, locationid, shelf, bin, quantity, rowguid, modifieddate from pr.pi""".as(PiRow.rowParser("").*)
  }
  override def selectByFieldValues(fieldValues: List[PiFieldOrIdValue[_]])(implicit c: Connection): List[PiRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case PiFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case PiFieldValue.productid(value) => NamedParameter("productid", ParameterValue.from(value))
          case PiFieldValue.locationid(value) => NamedParameter("locationid", ParameterValue.from(value))
          case PiFieldValue.shelf(value) => NamedParameter("shelf", ParameterValue.from(value))
          case PiFieldValue.bin(value) => NamedParameter("bin", ParameterValue.from(value))
          case PiFieldValue.quantity(value) => NamedParameter("quantity", ParameterValue.from(value))
          case PiFieldValue.rowguid(value) => NamedParameter("rowguid", ParameterValue.from(value))
          case PiFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select * from pr.pi where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}"""
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(PiRow.rowParser("").*)
    }
  
  }
}
