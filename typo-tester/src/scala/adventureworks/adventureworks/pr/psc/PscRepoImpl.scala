/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pr
package psc

import anorm.NamedParameter
import anorm.ParameterValue
import anorm.SqlStringInterpolation
import java.sql.Connection

object PscRepoImpl extends PscRepo {
  override def selectAll(implicit c: Connection): List[PscRow] = {
    SQL"""select id, productsubcategoryid, productcategoryid, name, rowguid, modifieddate from pr.psc""".as(PscRow.rowParser("").*)
  }
  override def selectByFieldValues(fieldValues: List[PscFieldOrIdValue[_]])(implicit c: Connection): List[PscRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case PscFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case PscFieldValue.productsubcategoryid(value) => NamedParameter("productsubcategoryid", ParameterValue.from(value))
          case PscFieldValue.productcategoryid(value) => NamedParameter("productcategoryid", ParameterValue.from(value))
          case PscFieldValue.name(value) => NamedParameter("name", ParameterValue.from(value))
          case PscFieldValue.rowguid(value) => NamedParameter("rowguid", ParameterValue.from(value))
          case PscFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select * from pr.psc where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}"""
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(PscRow.rowParser("").*)
    }
  
  }
}
