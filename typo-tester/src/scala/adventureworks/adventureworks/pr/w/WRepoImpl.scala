/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pr
package w

import adventureworks.production.product.ProductId
import adventureworks.production.scrapreason.ScrapreasonId
import adventureworks.production.workorder.WorkorderId
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime

object WRepoImpl extends WRepo {
  override def selectAll(implicit c: Connection): List[WRow] = {
    SQL"select id, workorderid, productid, orderqty, scrappedqty, startdate, enddate, duedate, scrapreasonid, modifieddate from pr.w".as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[WFieldOrIdValue[_]])(implicit c: Connection): List[WRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case WFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case WFieldValue.workorderid(value) => NamedParameter("workorderid", ParameterValue.from(value))
          case WFieldValue.productid(value) => NamedParameter("productid", ParameterValue.from(value))
          case WFieldValue.orderqty(value) => NamedParameter("orderqty", ParameterValue.from(value))
          case WFieldValue.scrappedqty(value) => NamedParameter("scrappedqty", ParameterValue.from(value))
          case WFieldValue.startdate(value) => NamedParameter("startdate", ParameterValue.from(value))
          case WFieldValue.enddate(value) => NamedParameter("enddate", ParameterValue.from(value))
          case WFieldValue.duedate(value) => NamedParameter("duedate", ParameterValue.from(value))
          case WFieldValue.scrapreasonid(value) => NamedParameter("scrapreasonid", ParameterValue.from(value))
          case WFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select * from pr.w where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}"""
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[WRow] =
    RowParser[WRow] { row =>
      Success(
        WRow(
          id = row[Option[Int]]("id"),
          workorderid = row[Option[WorkorderId]]("workorderid"),
          productid = row[Option[ProductId]]("productid"),
          orderqty = row[Option[Int]]("orderqty"),
          scrappedqty = row[Option[Int]]("scrappedqty"),
          startdate = row[Option[LocalDateTime]]("startdate"),
          enddate = row[Option[LocalDateTime]]("enddate"),
          duedate = row[Option[LocalDateTime]]("duedate"),
          scrapreasonid = row[Option[ScrapreasonId]]("scrapreasonid"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
