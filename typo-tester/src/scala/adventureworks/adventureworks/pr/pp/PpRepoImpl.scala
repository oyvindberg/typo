/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pr
package pp

import adventureworks.production.productphoto.ProductphotoId
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SqlStringInterpolation
import anorm.Success
import java.sql.Connection
import java.time.LocalDateTime

object PpRepoImpl extends PpRepo {
  override def selectAll(implicit c: Connection): List[PpRow] = {
    SQL"select id, productphotoid, thumbnailphoto, thumbnailphotofilename, largephoto, largephotofilename, modifieddate from pr.pp".as(rowParser.*)
  }
  override def selectByFieldValues(fieldValues: List[PpFieldOrIdValue[_]])(implicit c: Connection): List[PpRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case PpFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case PpFieldValue.productphotoid(value) => NamedParameter("productphotoid", ParameterValue.from(value))
          case PpFieldValue.thumbnailphoto(value) => NamedParameter("thumbnailphoto", ParameterValue.from(value))
          case PpFieldValue.thumbnailphotofilename(value) => NamedParameter("thumbnailphotofilename", ParameterValue.from(value))
          case PpFieldValue.largephoto(value) => NamedParameter("largephoto", ParameterValue.from(value))
          case PpFieldValue.largephotofilename(value) => NamedParameter("largephotofilename", ParameterValue.from(value))
          case PpFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select * from pr.pp where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}"""
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(rowParser.*)
    }
  
  }
  val rowParser: RowParser[PpRow] =
    RowParser[PpRow] { row =>
      Success(
        PpRow(
          id = row[Option[Int]]("id"),
          productphotoid = row[Option[ProductphotoId]]("productphotoid"),
          thumbnailphoto = row[Option[Byte]]("thumbnailphoto"),
          thumbnailphotofilename = row[Option[String]]("thumbnailphotofilename"),
          largephoto = row[Option[Byte]]("largephoto"),
          largephotofilename = row[Option[String]]("largephotofilename"),
          modifieddate = row[Option[LocalDateTime]]("modifieddate")
        )
      )
    }
}
