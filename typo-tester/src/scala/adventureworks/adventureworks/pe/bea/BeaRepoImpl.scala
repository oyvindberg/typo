/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package pe
package bea

import anorm.NamedParameter
import anorm.ParameterValue
import anorm.SqlStringInterpolation
import java.sql.Connection

object BeaRepoImpl extends BeaRepo {
  override def selectAll(implicit c: Connection): List[BeaRow] = {
    SQL"""select id, businessentityid, addressid, addresstypeid, rowguid, modifieddate from pe.bea""".as(BeaRow.rowParser("").*)
  }
  override def selectByFieldValues(fieldValues: List[BeaFieldOrIdValue[_]])(implicit c: Connection): List[BeaRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case BeaFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case BeaFieldValue.businessentityid(value) => NamedParameter("businessentityid", ParameterValue.from(value))
          case BeaFieldValue.addressid(value) => NamedParameter("addressid", ParameterValue.from(value))
          case BeaFieldValue.addresstypeid(value) => NamedParameter("addresstypeid", ParameterValue.from(value))
          case BeaFieldValue.rowguid(value) => NamedParameter("rowguid", ParameterValue.from(value))
          case BeaFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select * from pe.bea where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}"""
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(BeaRow.rowParser("").*)
    }
  
  }
}
