/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package hr
package d

import anorm.NamedParameter
import anorm.ParameterValue
import anorm.SqlStringInterpolation
import java.sql.Connection

object DRepoImpl extends DRepo {
  override def selectAll(implicit c: Connection): List[DRow] = {
    SQL"""select id, departmentid, name, groupname, modifieddate from hr.d""".as(DRow.rowParser("").*)
  }
  override def selectByFieldValues(fieldValues: List[DFieldOrIdValue[_]])(implicit c: Connection): List[DRow] = {
    fieldValues match {
      case Nil => selectAll
      case nonEmpty =>
        val namedParams = nonEmpty.map{
          case DFieldValue.id(value) => NamedParameter("id", ParameterValue.from(value))
          case DFieldValue.departmentid(value) => NamedParameter("departmentid", ParameterValue.from(value))
          case DFieldValue.name(value) => NamedParameter("name", ParameterValue.from(value))
          case DFieldValue.groupname(value) => NamedParameter("groupname", ParameterValue.from(value))
          case DFieldValue.modifieddate(value) => NamedParameter("modifieddate", ParameterValue.from(value))
        }
        val q = s"""select * from hr.d where ${namedParams.map(x => s"${x.name} = {${x.name}}").mkString(" AND ")}"""
        // this line is here to include an extension method which is only needed for scala 3. no import is emitted for `SQL` to avoid warning for scala 2
        import anorm._
        SQL(q)
          .on(namedParams: _*)
          .as(DRow.rowParser("").*)
    }
  
  }
}
