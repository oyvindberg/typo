/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks.production.workorder;

import java.sql.Connection;
import scala.annotation.nowarn;
import typo.dsl.DeleteBuilder;
import typo.dsl.DeleteBuilder.DeleteBuilderMock;
import typo.dsl.DeleteParams;
import typo.dsl.SelectBuilder;
import typo.dsl.SelectBuilderMock;
import typo.dsl.SelectParams;
import typo.dsl.UpdateBuilder;
import typo.dsl.UpdateBuilder.UpdateBuilderMock;
import typo.dsl.UpdateParams;

class WorkorderRepoMock(val toRow: Function1[WorkorderRowUnsaved, WorkorderRow], val map: scala.collection.mutable.Map[WorkorderId, WorkorderRow] = scala.collection.mutable.Map.empty) extends WorkorderRepo {
  def delete: DeleteBuilder[WorkorderFields, WorkorderRow] = DeleteBuilderMock(DeleteParams.empty, WorkorderFields.structure, map)
  def deleteById(workorderid: WorkorderId)(implicit c: Connection): Boolean = map.remove(workorderid).isDefined
  def deleteByIds(workorderids: Array[WorkorderId])(implicit c: Connection): Int = workorderids.map(id => map.remove(id)).count(_.isDefined)
  def insert(unsaved: WorkorderRow)(implicit c: Connection): WorkorderRow = {
    val _ = if (map.contains(unsaved.workorderid))
      sys.error(s"id ${unsaved.workorderid} already exists")
    else
      map.put(unsaved.workorderid, unsaved)
    
    unsaved
  }
  def insert(unsaved: WorkorderRowUnsaved)(implicit c: Connection): WorkorderRow = insert(toRow(unsaved))
  def insertStreaming(unsaved: Iterator[WorkorderRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.workorderid -> row)
    }
    unsaved.size.toLong
  }
  /** NOTE: this functionality requires PostgreSQL 16 or later! */
  def insertUnsavedStreaming(unsaved: Iterator[WorkorderRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.workorderid -> row)
    }
    unsaved.size.toLong
  }
  def select: SelectBuilder[WorkorderFields, WorkorderRow] = SelectBuilderMock(WorkorderFields.structure, () => map.values.toList, SelectParams.empty)
  def selectAll(implicit c: Connection): List[WorkorderRow] = map.values.toList
  def selectById(workorderid: WorkorderId)(implicit c: Connection): Option[WorkorderRow] = map.get(workorderid)
  def selectByIds(workorderids: Array[WorkorderId])(implicit c: Connection): List[WorkorderRow] = workorderids.flatMap(map.get).toList
  def selectByIdsTracked(workorderids: Array[WorkorderId])(implicit c: Connection): Map[WorkorderId, WorkorderRow] = {
    val byId = selectByIds(workorderids).view.map(x => (x.workorderid, x)).toMap
    workorderids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  def update: UpdateBuilder[WorkorderFields, WorkorderRow] = UpdateBuilderMock(UpdateParams.empty, WorkorderFields.structure, map)
  def update(row: WorkorderRow)(implicit c: Connection): Boolean = {
    map.get(row.workorderid) match {
      case Some(`row`) => false
      case Some(_) =>
        map.put(row.workorderid, row): @nowarn
        true
      case None => false
    }
  }
  def upsert(unsaved: WorkorderRow)(implicit c: Connection): WorkorderRow = {
    map.put(unsaved.workorderid, unsaved): @nowarn
    unsaved
  }
  def upsertBatch(unsaved: Iterable[WorkorderRow])(implicit c: Connection): List[WorkorderRow] = {
    unsaved.map { row =>
      map += (row.workorderid -> row)
      row
    }.toList
  }
  /** NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  def upsertStreaming(unsaved: Iterator[WorkorderRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.workorderid -> row)
    }
    unsaved.size
  }
}