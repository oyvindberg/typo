/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package customer

import adventureworks.customtypes.Defaulted
import adventureworks.frontpage.user.UserId
import anorm.Column
import anorm.RowParser
import anorm.Success
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: frontpage.customer
    Primary key: id */
case class CustomerRow(
  /** Default: gen_random_uuid() */
  id: CustomerId,
  /** Points to [[user.UserRow.id]] */
  userId: Option[UserId],
  companyName: Option[String],
  creditLimit: Option[BigDecimal],
  /** Default: false */
  verified: Option[Boolean]
){
   def toUnsavedRow(id: Defaulted[CustomerId], verified: Defaulted[Option[Boolean]] = Defaulted.Provided(this.verified)): CustomerRowUnsaved =
     CustomerRowUnsaved(userId, companyName, creditLimit, id, verified)
 }

object CustomerRow {
  implicit lazy val reads: Reads[CustomerRow] = Reads[CustomerRow](json => JsResult.fromTry(
      Try(
        CustomerRow(
          id = json.\("id").as(CustomerId.reads),
          userId = json.\("user_id").toOption.map(_.as(UserId.reads)),
          companyName = json.\("company_name").toOption.map(_.as(Reads.StringReads)),
          creditLimit = json.\("credit_limit").toOption.map(_.as(Reads.bigDecReads)),
          verified = json.\("verified").toOption.map(_.as(Reads.BooleanReads))
        )
      )
    ),
  )
  def rowParser(idx: Int): RowParser[CustomerRow] = RowParser[CustomerRow] { row =>
    Success(
      CustomerRow(
        id = row(idx + 0)(CustomerId.column),
        userId = row(idx + 1)(Column.columnToOption(UserId.column)),
        companyName = row(idx + 2)(Column.columnToOption(Column.columnToString)),
        creditLimit = row(idx + 3)(Column.columnToOption(Column.columnToScalaBigDecimal)),
        verified = row(idx + 4)(Column.columnToOption(Column.columnToBoolean))
      )
    )
  }
  implicit lazy val text: Text[CustomerRow] = Text.instance[CustomerRow]{ (row, sb) =>
    CustomerId.text.unsafeEncode(row.id, sb)
    sb.append(Text.DELIMETER)
    Text.option(UserId.text).unsafeEncode(row.userId, sb)
    sb.append(Text.DELIMETER)
    Text.option(Text.stringInstance).unsafeEncode(row.companyName, sb)
    sb.append(Text.DELIMETER)
    Text.option(Text.bigDecimalInstance).unsafeEncode(row.creditLimit, sb)
    sb.append(Text.DELIMETER)
    Text.option(Text.booleanInstance).unsafeEncode(row.verified, sb)
  }
  implicit lazy val writes: OWrites[CustomerRow] = OWrites[CustomerRow](o =>
    new JsObject(ListMap[String, JsValue](
      "id" -> CustomerId.writes.writes(o.id),
      "user_id" -> Writes.OptionWrites(UserId.writes).writes(o.userId),
      "company_name" -> Writes.OptionWrites(Writes.StringWrites).writes(o.companyName),
      "credit_limit" -> Writes.OptionWrites(Writes.BigDecimalWrites).writes(o.creditLimit),
      "verified" -> Writes.OptionWrites(Writes.BooleanWrites).writes(o.verified)
    ))
  )
}
