/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package person

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.frontpage.address.AddressId
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.SqlStringInterpolation
import anorm.ToStatement
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class PersonRepoImpl extends PersonRepo {
  override def delete: DeleteBuilder[PersonFields, PersonRow] = {
    DeleteBuilder(""""frontpage"."person"""", PersonFields.structure)
  }
  override def deleteById(id: PersonId)(implicit c: Connection): Boolean = {
    SQL"""delete from "frontpage"."person" where "id" = ${ParameterValue(id, null, PersonId.toStatement)}""".executeUpdate() > 0
  }
  override def deleteByIds(ids: Array[PersonId])(implicit c: Connection): Int = {
    SQL"""delete
          from "frontpage"."person"
          where "id" = ANY(${ids})
       """.executeUpdate()
    
  }
  override def insert(unsaved: PersonRow)(implicit c: Connection): PersonRow = {
    SQL"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
          values (${ParameterValue(unsaved.id, null, PersonId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}, ${ParameterValue(unsaved.addressId, null, ToStatement.optionToStatement(AddressId.toStatement, AddressId.parameterMetadata))}::uuid, ${ParameterValue(unsaved.createdAt, null, ToStatement.optionToStatement(TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp)
          returning "id", "name", "address_id", "created_at"::text
       """
      .executeInsert(PersonRow.rowParser(1).single)
    
  }
  override def insert(unsaved: PersonRowUnsaved)(implicit c: Connection): PersonRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      Some((NamedParameter("address_id", ParameterValue(unsaved.addressId, null, ToStatement.optionToStatement(AddressId.toStatement, AddressId.parameterMetadata))), "::uuid")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, PersonId.toStatement)), "::uuid"))
      },
      unsaved.createdAt match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("created_at", ParameterValue(value, null, ToStatement.optionToStatement(TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))), "::timestamp"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."person" default values
            returning "id", "name", "address_id", "created_at"::text
         """
        .executeInsert(PersonRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."person"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
                  values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
                  returning "id", "name", "address_id", "created_at"::text
               """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(PersonRow.rowParser(1).single)
    }
    
  }
  override def insertStreaming(unsaved: Iterator[PersonRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."person"("id", "name", "address_id", "created_at") FROM STDIN""", batchSize, unsaved)(PersonRow.text, c)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[PersonRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."person"("name", "address_id", "id", "created_at") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(PersonRowUnsaved.text, c)
  }
  override def select: SelectBuilder[PersonFields, PersonRow] = {
    SelectBuilderSql(""""frontpage"."person"""", PersonFields.structure, PersonRow.rowParser)
  }
  override def selectAll(implicit c: Connection): List[PersonRow] = {
    SQL"""select "id", "name", "address_id", "created_at"::text
          from "frontpage"."person"
       """.as(PersonRow.rowParser(1).*)
  }
  override def selectById(id: PersonId)(implicit c: Connection): Option[PersonRow] = {
    SQL"""select "id", "name", "address_id", "created_at"::text
          from "frontpage"."person"
          where "id" = ${ParameterValue(id, null, PersonId.toStatement)}
       """.as(PersonRow.rowParser(1).singleOpt)
  }
  override def selectByIds(ids: Array[PersonId])(implicit c: Connection): List[PersonRow] = {
    SQL"""select "id", "name", "address_id", "created_at"::text
          from "frontpage"."person"
          where "id" = ANY(${ids})
       """.as(PersonRow.rowParser(1).*)
    
  }
  override def selectByIdsTracked(ids: Array[PersonId])(implicit c: Connection): Map[PersonId, PersonRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[PersonFields, PersonRow] = {
    UpdateBuilder(""""frontpage"."person"""", PersonFields.structure, PersonRow.rowParser)
  }
  override def update(row: PersonRow)(implicit c: Connection): Boolean = {
    val id = row.id
    SQL"""update "frontpage"."person"
          set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)},
              "address_id" = ${ParameterValue(row.addressId, null, ToStatement.optionToStatement(AddressId.toStatement, AddressId.parameterMetadata))}::uuid,
              "created_at" = ${ParameterValue(row.createdAt, null, ToStatement.optionToStatement(TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
          where "id" = ${ParameterValue(id, null, PersonId.toStatement)}
       """.executeUpdate() > 0
  }
  override def upsert(unsaved: PersonRow)(implicit c: Connection): PersonRow = {
    SQL"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
          values (
            ${ParameterValue(unsaved.id, null, PersonId.toStatement)}::uuid,
            ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)},
            ${ParameterValue(unsaved.addressId, null, ToStatement.optionToStatement(AddressId.toStatement, AddressId.parameterMetadata))}::uuid,
            ${ParameterValue(unsaved.createdAt, null, ToStatement.optionToStatement(TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata))}::timestamp
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "address_id" = EXCLUDED."address_id",
            "created_at" = EXCLUDED."created_at"
          returning "id", "name", "address_id", "created_at"::text
       """
      .executeInsert(PersonRow.rowParser(1).single)
    
  }
  override def upsertBatch(unsaved: Iterable[PersonRow])(implicit c: Connection): List[PersonRow] = {
    def toNamedParameter(row: PersonRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, PersonId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement)),
      NamedParameter("address_id", ParameterValue(row.addressId, null, ToStatement.optionToStatement(AddressId.toStatement, AddressId.parameterMetadata))),
      NamedParameter("created_at", ParameterValue(row.createdAt, null, ToStatement.optionToStatement(TypoLocalDateTime.toStatement, TypoLocalDateTime.parameterMetadata)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
                values ({id}::uuid, {name}, {address_id}::uuid, {created_at}::timestamp)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name",
                  "address_id" = EXCLUDED."address_id",
                  "created_at" = EXCLUDED."created_at"
                returning "id", "name", "address_id", "created_at"::text
             """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(PersonRow.rowParser(1).*)
    }
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[PersonRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    SQL"""create temporary table person_TEMP (like "frontpage"."person") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy person_TEMP("id", "name", "address_id", "created_at") from stdin""", batchSize, unsaved)(PersonRow.text, c): @nowarn
    SQL"""insert into "frontpage"."person"("id", "name", "address_id", "created_at")
          select * from person_TEMP
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "address_id" = EXCLUDED."address_id",
            "created_at" = EXCLUDED."created_at"
          ;
          drop table person_TEMP;""".executeUpdate()
  }
}
