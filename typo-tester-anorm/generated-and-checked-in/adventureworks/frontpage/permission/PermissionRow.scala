/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package permission

import adventureworks.customtypes.Defaulted
import anorm.Column
import anorm.RowParser
import anorm.Success
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: frontpage.permission
    Primary key: id */
case class PermissionRow(
  /** Default: gen_random_uuid() */
  id: PermissionId,
  name: String
){
   def toUnsavedRow(id: Defaulted[PermissionId]): PermissionRowUnsaved =
     PermissionRowUnsaved(name, id)
 }

object PermissionRow {
  implicit lazy val reads: Reads[PermissionRow] = Reads[PermissionRow](json => JsResult.fromTry(
      Try(
        PermissionRow(
          id = json.\("id").as(PermissionId.reads),
          name = json.\("name").as(Reads.StringReads)
        )
      )
    ),
  )
  def rowParser(idx: Int): RowParser[PermissionRow] = RowParser[PermissionRow] { row =>
    Success(
      PermissionRow(
        id = row(idx + 0)(PermissionId.column),
        name = row(idx + 1)(Column.columnToString)
      )
    )
  }
  implicit lazy val text: Text[PermissionRow] = Text.instance[PermissionRow]{ (row, sb) =>
    PermissionId.text.unsafeEncode(row.id, sb)
    sb.append(Text.DELIMETER)
    Text.stringInstance.unsafeEncode(row.name, sb)
  }
  implicit lazy val writes: OWrites[PermissionRow] = OWrites[PermissionRow](o =>
    new JsObject(ListMap[String, JsValue](
      "id" -> PermissionId.writes.writes(o.id),
      "name" -> Writes.StringWrites.writes(o.name)
    ))
  )
}
