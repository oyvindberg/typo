/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package permission

import adventureworks.customtypes.Defaulted
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.SqlStringInterpolation
import anorm.ToStatement
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class PermissionRepoImpl extends PermissionRepo {
  override def delete: DeleteBuilder[PermissionFields, PermissionRow] = {
    DeleteBuilder(""""frontpage"."permission"""", PermissionFields.structure)
  }
  override def deleteById(id: PermissionId)(implicit c: Connection): Boolean = {
    SQL"""delete from "frontpage"."permission" where "id" = ${ParameterValue(id, null, PermissionId.toStatement)}""".executeUpdate() > 0
  }
  override def deleteByIds(ids: Array[PermissionId])(implicit c: Connection): Int = {
    SQL"""delete
          from "frontpage"."permission"
          where "id" = ANY(${ids})
       """.executeUpdate()
    
  }
  override def insert(unsaved: PermissionRow)(implicit c: Connection): PermissionRow = {
    SQL"""insert into "frontpage"."permission"("id", "name")
          values (${ParameterValue(unsaved.id, null, PermissionId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)})
          returning "id", "name"
       """
      .executeInsert(PermissionRow.rowParser(1).single)
    
  }
  override def insert(unsaved: PermissionRowUnsaved)(implicit c: Connection): PermissionRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, PermissionId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."permission" default values
            returning "id", "name"
         """
        .executeInsert(PermissionRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."permission"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
                  values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
                  returning "id", "name"
               """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(PermissionRow.rowParser(1).single)
    }
    
  }
  override def insertStreaming(unsaved: Iterator[PermissionRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."permission"("id", "name") FROM STDIN""", batchSize, unsaved)(PermissionRow.text, c)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[PermissionRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."permission"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(PermissionRowUnsaved.text, c)
  }
  override def select: SelectBuilder[PermissionFields, PermissionRow] = {
    SelectBuilderSql(""""frontpage"."permission"""", PermissionFields.structure, PermissionRow.rowParser)
  }
  override def selectAll(implicit c: Connection): List[PermissionRow] = {
    SQL"""select "id", "name"
          from "frontpage"."permission"
       """.as(PermissionRow.rowParser(1).*)
  }
  override def selectById(id: PermissionId)(implicit c: Connection): Option[PermissionRow] = {
    SQL"""select "id", "name"
          from "frontpage"."permission"
          where "id" = ${ParameterValue(id, null, PermissionId.toStatement)}
       """.as(PermissionRow.rowParser(1).singleOpt)
  }
  override def selectByIds(ids: Array[PermissionId])(implicit c: Connection): List[PermissionRow] = {
    SQL"""select "id", "name"
          from "frontpage"."permission"
          where "id" = ANY(${ids})
       """.as(PermissionRow.rowParser(1).*)
    
  }
  override def selectByIdsTracked(ids: Array[PermissionId])(implicit c: Connection): Map[PermissionId, PermissionRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def selectByUniqueName(name: String)(implicit c: Connection): Option[PermissionRow] = {
    SQL"""select "id", "name"
          from "frontpage"."permission"
          where "name" = ${ParameterValue(name, null, ToStatement.stringToStatement)}
       """.as(PermissionRow.rowParser(1).singleOpt)
    
  }
  override def update: UpdateBuilder[PermissionFields, PermissionRow] = {
    UpdateBuilder(""""frontpage"."permission"""", PermissionFields.structure, PermissionRow.rowParser)
  }
  override def update(row: PermissionRow)(implicit c: Connection): Boolean = {
    val id = row.id
    SQL"""update "frontpage"."permission"
          set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)}
          where "id" = ${ParameterValue(id, null, PermissionId.toStatement)}
       """.executeUpdate() > 0
  }
  override def upsert(unsaved: PermissionRow)(implicit c: Connection): PermissionRow = {
    SQL"""insert into "frontpage"."permission"("id", "name")
          values (
            ${ParameterValue(unsaved.id, null, PermissionId.toStatement)}::uuid,
            ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name"
          returning "id", "name"
       """
      .executeInsert(PermissionRow.rowParser(1).single)
    
  }
  override def upsertBatch(unsaved: Iterable[PermissionRow])(implicit c: Connection): List[PermissionRow] = {
    def toNamedParameter(row: PermissionRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, PermissionId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."permission"("id", "name")
                values ({id}::uuid, {name})
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name"
                returning "id", "name"
             """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(PermissionRow.rowParser(1).*)
    }
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[PermissionRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    SQL"""create temporary table permission_TEMP (like "frontpage"."permission") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy permission_TEMP("id", "name") from stdin""", batchSize, unsaved)(PermissionRow.text, c): @nowarn
    SQL"""insert into "frontpage"."permission"("id", "name")
          select * from permission_TEMP
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name"
          ;
          drop table permission_TEMP;""".executeUpdate()
  }
}
