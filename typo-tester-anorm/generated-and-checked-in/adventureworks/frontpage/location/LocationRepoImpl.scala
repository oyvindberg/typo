/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package location

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoInet
import adventureworks.customtypes.TypoJsonb
import adventureworks.customtypes.TypoPoint
import adventureworks.customtypes.TypoPolygon
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.SqlStringInterpolation
import anorm.ToStatement
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class LocationRepoImpl extends LocationRepo {
  override def delete: DeleteBuilder[LocationFields, LocationRow] = {
    DeleteBuilder(""""frontpage"."location"""", LocationFields.structure)
  }
  override def deleteById(id: LocationId)(implicit c: Connection): Boolean = {
    SQL"""delete from "frontpage"."location" where "id" = ${ParameterValue(id, null, LocationId.toStatement)}""".executeUpdate() > 0
  }
  override def deleteByIds(ids: Array[LocationId])(implicit c: Connection): Int = {
    SQL"""delete
          from "frontpage"."location"
          where "id" = ANY(${ids})
       """.executeUpdate()
    
  }
  override def insert(unsaved: LocationRow)(implicit c: Connection): LocationRow = {
    SQL"""insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
          values (${ParameterValue(unsaved.id, null, LocationId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}, ${ParameterValue(unsaved.position, null, ToStatement.optionToStatement(TypoPoint.toStatement, TypoPoint.parameterMetadata))}::point, ${ParameterValue(unsaved.area, null, ToStatement.optionToStatement(TypoPolygon.toStatement, TypoPolygon.parameterMetadata))}::polygon, ${ParameterValue(unsaved.ipRange, null, ToStatement.optionToStatement(TypoInet.toStatement, TypoInet.parameterMetadata))}::inet, ${ParameterValue(unsaved.metadata, null, ToStatement.optionToStatement(TypoJsonb.toStatement, TypoJsonb.parameterMetadata))}::jsonb)
          returning "id", "name", "position", "area", "ip_range", "metadata"
       """
      .executeInsert(LocationRow.rowParser(1).single)
    
  }
  override def insert(unsaved: LocationRowUnsaved)(implicit c: Connection): LocationRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      Some((NamedParameter("position", ParameterValue(unsaved.position, null, ToStatement.optionToStatement(TypoPoint.toStatement, TypoPoint.parameterMetadata))), "::point")),
      Some((NamedParameter("area", ParameterValue(unsaved.area, null, ToStatement.optionToStatement(TypoPolygon.toStatement, TypoPolygon.parameterMetadata))), "::polygon")),
      Some((NamedParameter("ip_range", ParameterValue(unsaved.ipRange, null, ToStatement.optionToStatement(TypoInet.toStatement, TypoInet.parameterMetadata))), "::inet")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, LocationId.toStatement)), "::uuid"))
      },
      unsaved.metadata match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("metadata", ParameterValue(value, null, ToStatement.optionToStatement(TypoJsonb.toStatement, TypoJsonb.parameterMetadata))), "::jsonb"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."location" default values
            returning "id", "name", "position", "area", "ip_range", "metadata"
         """
        .executeInsert(LocationRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."location"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
                  values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
                  returning "id", "name", "position", "area", "ip_range", "metadata"
               """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(LocationRow.rowParser(1).single)
    }
    
  }
  override def insertStreaming(unsaved: Iterator[LocationRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata") FROM STDIN""", batchSize, unsaved)(LocationRow.text, c)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[LocationRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."location"("name", "position", "area", "ip_range", "id", "metadata") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(LocationRowUnsaved.text, c)
  }
  override def select: SelectBuilder[LocationFields, LocationRow] = {
    SelectBuilderSql(""""frontpage"."location"""", LocationFields.structure, LocationRow.rowParser)
  }
  override def selectAll(implicit c: Connection): List[LocationRow] = {
    SQL"""select "id", "name", "position", "area", "ip_range", "metadata"
          from "frontpage"."location"
       """.as(LocationRow.rowParser(1).*)
  }
  override def selectById(id: LocationId)(implicit c: Connection): Option[LocationRow] = {
    SQL"""select "id", "name", "position", "area", "ip_range", "metadata"
          from "frontpage"."location"
          where "id" = ${ParameterValue(id, null, LocationId.toStatement)}
       """.as(LocationRow.rowParser(1).singleOpt)
  }
  override def selectByIds(ids: Array[LocationId])(implicit c: Connection): List[LocationRow] = {
    SQL"""select "id", "name", "position", "area", "ip_range", "metadata"
          from "frontpage"."location"
          where "id" = ANY(${ids})
       """.as(LocationRow.rowParser(1).*)
    
  }
  override def selectByIdsTracked(ids: Array[LocationId])(implicit c: Connection): Map[LocationId, LocationRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[LocationFields, LocationRow] = {
    UpdateBuilder(""""frontpage"."location"""", LocationFields.structure, LocationRow.rowParser)
  }
  override def update(row: LocationRow)(implicit c: Connection): Boolean = {
    val id = row.id
    SQL"""update "frontpage"."location"
          set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)},
              "position" = ${ParameterValue(row.position, null, ToStatement.optionToStatement(TypoPoint.toStatement, TypoPoint.parameterMetadata))}::point,
              "area" = ${ParameterValue(row.area, null, ToStatement.optionToStatement(TypoPolygon.toStatement, TypoPolygon.parameterMetadata))}::polygon,
              "ip_range" = ${ParameterValue(row.ipRange, null, ToStatement.optionToStatement(TypoInet.toStatement, TypoInet.parameterMetadata))}::inet,
              "metadata" = ${ParameterValue(row.metadata, null, ToStatement.optionToStatement(TypoJsonb.toStatement, TypoJsonb.parameterMetadata))}::jsonb
          where "id" = ${ParameterValue(id, null, LocationId.toStatement)}
       """.executeUpdate() > 0
  }
  override def upsert(unsaved: LocationRow)(implicit c: Connection): LocationRow = {
    SQL"""insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
          values (
            ${ParameterValue(unsaved.id, null, LocationId.toStatement)}::uuid,
            ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)},
            ${ParameterValue(unsaved.position, null, ToStatement.optionToStatement(TypoPoint.toStatement, TypoPoint.parameterMetadata))}::point,
            ${ParameterValue(unsaved.area, null, ToStatement.optionToStatement(TypoPolygon.toStatement, TypoPolygon.parameterMetadata))}::polygon,
            ${ParameterValue(unsaved.ipRange, null, ToStatement.optionToStatement(TypoInet.toStatement, TypoInet.parameterMetadata))}::inet,
            ${ParameterValue(unsaved.metadata, null, ToStatement.optionToStatement(TypoJsonb.toStatement, TypoJsonb.parameterMetadata))}::jsonb
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "position" = EXCLUDED."position",
            "area" = EXCLUDED."area",
            "ip_range" = EXCLUDED."ip_range",
            "metadata" = EXCLUDED."metadata"
          returning "id", "name", "position", "area", "ip_range", "metadata"
       """
      .executeInsert(LocationRow.rowParser(1).single)
    
  }
  override def upsertBatch(unsaved: Iterable[LocationRow])(implicit c: Connection): List[LocationRow] = {
    def toNamedParameter(row: LocationRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, LocationId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement)),
      NamedParameter("position", ParameterValue(row.position, null, ToStatement.optionToStatement(TypoPoint.toStatement, TypoPoint.parameterMetadata))),
      NamedParameter("area", ParameterValue(row.area, null, ToStatement.optionToStatement(TypoPolygon.toStatement, TypoPolygon.parameterMetadata))),
      NamedParameter("ip_range", ParameterValue(row.ipRange, null, ToStatement.optionToStatement(TypoInet.toStatement, TypoInet.parameterMetadata))),
      NamedParameter("metadata", ParameterValue(row.metadata, null, ToStatement.optionToStatement(TypoJsonb.toStatement, TypoJsonb.parameterMetadata)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
                values ({id}::uuid, {name}, {position}::point, {area}::polygon, {ip_range}::inet, {metadata}::jsonb)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name",
                  "position" = EXCLUDED."position",
                  "area" = EXCLUDED."area",
                  "ip_range" = EXCLUDED."ip_range",
                  "metadata" = EXCLUDED."metadata"
                returning "id", "name", "position", "area", "ip_range", "metadata"
             """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(LocationRow.rowParser(1).*)
    }
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[LocationRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    SQL"""create temporary table location_TEMP (like "frontpage"."location") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy location_TEMP("id", "name", "position", "area", "ip_range", "metadata") from stdin""", batchSize, unsaved)(LocationRow.text, c): @nowarn
    SQL"""insert into "frontpage"."location"("id", "name", "position", "area", "ip_range", "metadata")
          select * from location_TEMP
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "position" = EXCLUDED."position",
            "area" = EXCLUDED."area",
            "ip_range" = EXCLUDED."ip_range",
            "metadata" = EXCLUDED."metadata"
          ;
          drop table location_TEMP;""".executeUpdate()
  }
}
