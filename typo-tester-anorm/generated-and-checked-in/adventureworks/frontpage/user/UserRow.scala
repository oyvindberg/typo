/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package user

import adventureworks.customtypes.Defaulted
import adventureworks.customtypes.TypoLocalDateTime
import adventureworks.frontpage.department.DepartmentId
import anorm.Column
import anorm.RowParser
import anorm.Success
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.OWrites
import play.api.libs.json.Reads
import play.api.libs.json.Writes
import scala.collection.immutable.ListMap
import scala.util.Try

/** Table: frontpage.user
    Primary key: id */
case class UserRow(
  /** Default: gen_random_uuid() */
  id: UserId,
  email: Email,
  name: String,
  /** Default: now() */
  createdAt: Option[TypoLocalDateTime],
  /** Points to [[department.DepartmentRow.id]] */
  departmentId: Option[DepartmentId],
  /** Default: 'active'::frontpage.user_status */
  status: Option[UserStatus],
  /** Default: false */
  verified: Option[Boolean],
  /** Points to [[UserRow.id]] */
  managerId: Option[UserId],
  /** Default: 'employee'::frontpage.user_role */
  role: Option[UserRole]
){
   def toUnsavedRow(id: Defaulted[UserId], createdAt: Defaulted[Option[TypoLocalDateTime]] = Defaulted.Provided(this.createdAt), status: Defaulted[Option[UserStatus]] = Defaulted.Provided(this.status), verified: Defaulted[Option[Boolean]] = Defaulted.Provided(this.verified), role: Defaulted[Option[UserRole]] = Defaulted.Provided(this.role)): UserRowUnsaved =
     UserRowUnsaved(email, name, departmentId, managerId, id, createdAt, status, verified, role)
 }

object UserRow {
  implicit lazy val reads: Reads[UserRow] = Reads[UserRow](json => JsResult.fromTry(
      Try(
        UserRow(
          id = json.\("id").as(UserId.reads),
          email = json.\("email").as(Email.reads),
          name = json.\("name").as(Reads.StringReads),
          createdAt = json.\("created_at").toOption.map(_.as(TypoLocalDateTime.reads)),
          departmentId = json.\("department_id").toOption.map(_.as(DepartmentId.reads)),
          status = json.\("status").toOption.map(_.as(UserStatus.reads)),
          verified = json.\("verified").toOption.map(_.as(Reads.BooleanReads)),
          managerId = json.\("manager_id").toOption.map(_.as(UserId.reads)),
          role = json.\("role").toOption.map(_.as(UserRole.reads))
        )
      )
    ),
  )
  def rowParser(idx: Int): RowParser[UserRow] = RowParser[UserRow] { row =>
    Success(
      UserRow(
        id = row(idx + 0)(UserId.column),
        email = row(idx + 1)(Email.column),
        name = row(idx + 2)(Column.columnToString),
        createdAt = row(idx + 3)(Column.columnToOption(TypoLocalDateTime.column)),
        departmentId = row(idx + 4)(Column.columnToOption(DepartmentId.column)),
        status = row(idx + 5)(Column.columnToOption(UserStatus.column)),
        verified = row(idx + 6)(Column.columnToOption(Column.columnToBoolean)),
        managerId = row(idx + 7)(Column.columnToOption(UserId.column)),
        role = row(idx + 8)(Column.columnToOption(UserRole.column))
      )
    )
  }
  implicit lazy val text: Text[UserRow] = Text.instance[UserRow]{ (row, sb) =>
    UserId.text.unsafeEncode(row.id, sb)
    sb.append(Text.DELIMETER)
    Email.text.unsafeEncode(row.email, sb)
    sb.append(Text.DELIMETER)
    Text.stringInstance.unsafeEncode(row.name, sb)
    sb.append(Text.DELIMETER)
    Text.option(TypoLocalDateTime.text).unsafeEncode(row.createdAt, sb)
    sb.append(Text.DELIMETER)
    Text.option(DepartmentId.text).unsafeEncode(row.departmentId, sb)
    sb.append(Text.DELIMETER)
    Text.option(UserStatus.text).unsafeEncode(row.status, sb)
    sb.append(Text.DELIMETER)
    Text.option(Text.booleanInstance).unsafeEncode(row.verified, sb)
    sb.append(Text.DELIMETER)
    Text.option(UserId.text).unsafeEncode(row.managerId, sb)
    sb.append(Text.DELIMETER)
    Text.option(UserRole.text).unsafeEncode(row.role, sb)
  }
  implicit lazy val writes: OWrites[UserRow] = OWrites[UserRow](o =>
    new JsObject(ListMap[String, JsValue](
      "id" -> UserId.writes.writes(o.id),
      "email" -> Email.writes.writes(o.email),
      "name" -> Writes.StringWrites.writes(o.name),
      "created_at" -> Writes.OptionWrites(TypoLocalDateTime.writes).writes(o.createdAt),
      "department_id" -> Writes.OptionWrites(DepartmentId.writes).writes(o.departmentId),
      "status" -> Writes.OptionWrites(UserStatus.writes).writes(o.status),
      "verified" -> Writes.OptionWrites(Writes.BooleanWrites).writes(o.verified),
      "manager_id" -> Writes.OptionWrites(UserId.writes).writes(o.managerId),
      "role" -> Writes.OptionWrites(UserRole.writes).writes(o.role)
    ))
  )
}
