/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package company

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

class CompanyRepoMock(toRow: Function1[CompanyRowUnsaved, CompanyRow],
                      map: scala.collection.mutable.Map[CompanyId, CompanyRow] = scala.collection.mutable.Map.empty) extends CompanyRepo {
  override def delete: DeleteBuilder[CompanyFields, CompanyRow] = {
    DeleteBuilderMock(DeleteParams.empty, CompanyFields.structure, map)
  }
  override def deleteById(id: CompanyId)(implicit c: Connection): Boolean = {
    map.remove(id).isDefined
  }
  override def deleteByIds(ids: Array[CompanyId])(implicit c: Connection): Int = {
    ids.map(id => map.remove(id)).count(_.isDefined)
  }
  override def insert(unsaved: CompanyRow)(implicit c: Connection): CompanyRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
    
    unsaved
  }
  override def insert(unsaved: CompanyRowUnsaved)(implicit c: Connection): CompanyRow = {
    insert(toRow(unsaved))
  }
  override def insertStreaming(unsaved: Iterator[CompanyRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[CompanyRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }
  override def select: SelectBuilder[CompanyFields, CompanyRow] = {
    SelectBuilderMock(CompanyFields.structure, () => map.values.toList, SelectParams.empty)
  }
  override def selectAll(implicit c: Connection): List[CompanyRow] = {
    map.values.toList
  }
  override def selectById(id: CompanyId)(implicit c: Connection): Option[CompanyRow] = {
    map.get(id)
  }
  override def selectByIds(ids: Array[CompanyId])(implicit c: Connection): List[CompanyRow] = {
    ids.flatMap(map.get).toList
  }
  override def selectByIdsTracked(ids: Array[CompanyId])(implicit c: Connection): Map[CompanyId, CompanyRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[CompanyFields, CompanyRow] = {
    UpdateBuilderMock(UpdateParams.empty, CompanyFields.structure, map)
  }
  override def update(row: CompanyRow)(implicit c: Connection): Boolean = {
    map.get(row.id) match {
      case Some(`row`) => false
      case Some(_) =>
        map.put(row.id, row): @nowarn
        true
      case None => false
    }
  }
  override def upsert(unsaved: CompanyRow)(implicit c: Connection): CompanyRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }
  override def upsertBatch(unsaved: Iterable[CompanyRow])(implicit c: Connection): List[CompanyRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[CompanyRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}
