/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package company

import adventureworks.customtypes.Defaulted
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.SqlStringInterpolation
import anorm.ToStatement
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class CompanyRepoImpl extends CompanyRepo {
  override def delete: DeleteBuilder[CompanyFields, CompanyRow] = {
    DeleteBuilder(""""frontpage"."company"""", CompanyFields.structure)
  }
  override def deleteById(id: CompanyId)(implicit c: Connection): Boolean = {
    SQL"""delete from "frontpage"."company" where "id" = ${ParameterValue(id, null, CompanyId.toStatement)}""".executeUpdate() > 0
  }
  override def deleteByIds(ids: Array[CompanyId])(implicit c: Connection): Int = {
    SQL"""delete
          from "frontpage"."company"
          where "id" = ANY(${ids})
       """.executeUpdate()
    
  }
  override def insert(unsaved: CompanyRow)(implicit c: Connection): CompanyRow = {
    SQL"""insert into "frontpage"."company"("id", "name")
          values (${ParameterValue(unsaved.id, null, CompanyId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)})
          returning "id", "name"
       """
      .executeInsert(CompanyRow.rowParser(1).single)
    
  }
  override def insert(unsaved: CompanyRowUnsaved)(implicit c: Connection): CompanyRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, CompanyId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."company" default values
            returning "id", "name"
         """
        .executeInsert(CompanyRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."company"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
                  values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
                  returning "id", "name"
               """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(CompanyRow.rowParser(1).single)
    }
    
  }
  override def insertStreaming(unsaved: Iterator[CompanyRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."company"("id", "name") FROM STDIN""", batchSize, unsaved)(CompanyRow.text, c)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[CompanyRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."company"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(CompanyRowUnsaved.text, c)
  }
  override def select: SelectBuilder[CompanyFields, CompanyRow] = {
    SelectBuilderSql(""""frontpage"."company"""", CompanyFields.structure, CompanyRow.rowParser)
  }
  override def selectAll(implicit c: Connection): List[CompanyRow] = {
    SQL"""select "id", "name"
          from "frontpage"."company"
       """.as(CompanyRow.rowParser(1).*)
  }
  override def selectById(id: CompanyId)(implicit c: Connection): Option[CompanyRow] = {
    SQL"""select "id", "name"
          from "frontpage"."company"
          where "id" = ${ParameterValue(id, null, CompanyId.toStatement)}
       """.as(CompanyRow.rowParser(1).singleOpt)
  }
  override def selectByIds(ids: Array[CompanyId])(implicit c: Connection): List[CompanyRow] = {
    SQL"""select "id", "name"
          from "frontpage"."company"
          where "id" = ANY(${ids})
       """.as(CompanyRow.rowParser(1).*)
    
  }
  override def selectByIdsTracked(ids: Array[CompanyId])(implicit c: Connection): Map[CompanyId, CompanyRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[CompanyFields, CompanyRow] = {
    UpdateBuilder(""""frontpage"."company"""", CompanyFields.structure, CompanyRow.rowParser)
  }
  override def update(row: CompanyRow)(implicit c: Connection): Boolean = {
    val id = row.id
    SQL"""update "frontpage"."company"
          set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)}
          where "id" = ${ParameterValue(id, null, CompanyId.toStatement)}
       """.executeUpdate() > 0
  }
  override def upsert(unsaved: CompanyRow)(implicit c: Connection): CompanyRow = {
    SQL"""insert into "frontpage"."company"("id", "name")
          values (
            ${ParameterValue(unsaved.id, null, CompanyId.toStatement)}::uuid,
            ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name"
          returning "id", "name"
       """
      .executeInsert(CompanyRow.rowParser(1).single)
    
  }
  override def upsertBatch(unsaved: Iterable[CompanyRow])(implicit c: Connection): List[CompanyRow] = {
    def toNamedParameter(row: CompanyRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, CompanyId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."company"("id", "name")
                values ({id}::uuid, {name})
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name"
                returning "id", "name"
             """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(CompanyRow.rowParser(1).*)
    }
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[CompanyRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    SQL"""create temporary table company_TEMP (like "frontpage"."company") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy company_TEMP("id", "name") from stdin""", batchSize, unsaved)(CompanyRow.text, c): @nowarn
    SQL"""insert into "frontpage"."company"("id", "name")
          select * from company_TEMP
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name"
          ;
          drop table company_TEMP;""".executeUpdate()
  }
}
