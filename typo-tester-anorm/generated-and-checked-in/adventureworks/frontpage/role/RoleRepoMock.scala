/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package role

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

class RoleRepoMock(toRow: Function1[RoleRowUnsaved, RoleRow],
                   map: scala.collection.mutable.Map[RoleId, RoleRow] = scala.collection.mutable.Map.empty) extends RoleRepo {
  override def delete: DeleteBuilder[RoleFields, RoleRow] = {
    DeleteBuilderMock(DeleteParams.empty, RoleFields.structure, map)
  }
  override def deleteById(id: RoleId)(implicit c: Connection): Boolean = {
    map.remove(id).isDefined
  }
  override def deleteByIds(ids: Array[RoleId])(implicit c: Connection): Int = {
    ids.map(id => map.remove(id)).count(_.isDefined)
  }
  override def insert(unsaved: RoleRow)(implicit c: Connection): RoleRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
    
    unsaved
  }
  override def insert(unsaved: RoleRowUnsaved)(implicit c: Connection): RoleRow = {
    insert(toRow(unsaved))
  }
  override def insertStreaming(unsaved: Iterator[RoleRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[RoleRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }
  override def select: SelectBuilder[RoleFields, RoleRow] = {
    SelectBuilderMock(RoleFields.structure, () => map.values.toList, SelectParams.empty)
  }
  override def selectAll(implicit c: Connection): List[RoleRow] = {
    map.values.toList
  }
  override def selectById(id: RoleId)(implicit c: Connection): Option[RoleRow] = {
    map.get(id)
  }
  override def selectByIds(ids: Array[RoleId])(implicit c: Connection): List[RoleRow] = {
    ids.flatMap(map.get).toList
  }
  override def selectByIdsTracked(ids: Array[RoleId])(implicit c: Connection): Map[RoleId, RoleRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def selectByUniqueName(name: String)(implicit c: Connection): Option[RoleRow] = {
    map.values.find(v => name == v.name)
  }
  override def update: UpdateBuilder[RoleFields, RoleRow] = {
    UpdateBuilderMock(UpdateParams.empty, RoleFields.structure, map)
  }
  override def update(row: RoleRow)(implicit c: Connection): Boolean = {
    map.get(row.id) match {
      case Some(`row`) => false
      case Some(_) =>
        map.put(row.id, row): @nowarn
        true
      case None => false
    }
  }
  override def upsert(unsaved: RoleRow)(implicit c: Connection): RoleRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }
  override def upsertBatch(unsaved: Iterable[RoleRow])(implicit c: Connection): List[RoleRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[RoleRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}
