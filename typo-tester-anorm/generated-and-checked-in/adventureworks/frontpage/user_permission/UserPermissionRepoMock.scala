/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package user_permission

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

class UserPermissionRepoMock(toRow: Function1[UserPermissionRowUnsaved, UserPermissionRow],
                             map: scala.collection.mutable.Map[UserPermissionId, UserPermissionRow] = scala.collection.mutable.Map.empty) extends UserPermissionRepo {
  override def delete: DeleteBuilder[UserPermissionFields, UserPermissionRow] = {
    DeleteBuilderMock(DeleteParams.empty, UserPermissionFields.structure, map)
  }
  override def deleteById(compositeId: UserPermissionId)(implicit c: Connection): Boolean = {
    map.remove(compositeId).isDefined
  }
  override def deleteByIds(compositeIds: Array[UserPermissionId])(implicit c: Connection): Int = {
    compositeIds.map(id => map.remove(id)).count(_.isDefined)
  }
  override def insert(unsaved: UserPermissionRow)(implicit c: Connection): UserPermissionRow = {
    val _ = if (map.contains(unsaved.compositeId))
      sys.error(s"id ${unsaved.compositeId} already exists")
    else
      map.put(unsaved.compositeId, unsaved)
    
    unsaved
  }
  override def insert(unsaved: UserPermissionRowUnsaved)(implicit c: Connection): UserPermissionRow = {
    insert(toRow(unsaved))
  }
  override def insertStreaming(unsaved: Iterator[UserPermissionRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[UserPermissionRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.compositeId -> row)
    }
    unsaved.size.toLong
  }
  override def select: SelectBuilder[UserPermissionFields, UserPermissionRow] = {
    SelectBuilderMock(UserPermissionFields.structure, () => map.values.toList, SelectParams.empty)
  }
  override def selectAll(implicit c: Connection): List[UserPermissionRow] = {
    map.values.toList
  }
  override def selectById(compositeId: UserPermissionId)(implicit c: Connection): Option[UserPermissionRow] = {
    map.get(compositeId)
  }
  override def selectByIds(compositeIds: Array[UserPermissionId])(implicit c: Connection): List[UserPermissionRow] = {
    compositeIds.flatMap(map.get).toList
  }
  override def selectByIdsTracked(compositeIds: Array[UserPermissionId])(implicit c: Connection): Map[UserPermissionId, UserPermissionRow] = {
    val byId = selectByIds(compositeIds).view.map(x => (x.compositeId, x)).toMap
    compositeIds.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[UserPermissionFields, UserPermissionRow] = {
    UpdateBuilderMock(UpdateParams.empty, UserPermissionFields.structure, map)
  }
  override def update(row: UserPermissionRow)(implicit c: Connection): Boolean = {
    map.get(row.compositeId) match {
      case Some(`row`) => false
      case Some(_) =>
        map.put(row.compositeId, row): @nowarn
        true
      case None => false
    }
  }
  override def upsert(unsaved: UserPermissionRow)(implicit c: Connection): UserPermissionRow = {
    map.put(unsaved.compositeId, unsaved): @nowarn
    unsaved
  }
  override def upsertBatch(unsaved: Iterable[UserPermissionRow])(implicit c: Connection): List[UserPermissionRow] = {
    unsaved.map { row =>
      map += (row.compositeId -> row)
      row
    }.toList
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[UserPermissionRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.compositeId -> row)
    }
    unsaved.size
  }
}
