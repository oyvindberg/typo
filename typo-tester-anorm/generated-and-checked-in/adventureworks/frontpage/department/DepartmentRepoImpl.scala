/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package department

import adventureworks.customtypes.Defaulted
import adventureworks.frontpage.company.CompanyId
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterMetaData
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.SqlStringInterpolation
import anorm.ToStatement
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class DepartmentRepoImpl extends DepartmentRepo {
  override def delete: DeleteBuilder[DepartmentFields, DepartmentRow] = {
    DeleteBuilder(""""frontpage"."department"""", DepartmentFields.structure)
  }
  override def deleteById(id: DepartmentId)(implicit c: Connection): Boolean = {
    SQL"""delete from "frontpage"."department" where "id" = ${ParameterValue(id, null, DepartmentId.toStatement)}""".executeUpdate() > 0
  }
  override def deleteByIds(ids: Array[DepartmentId])(implicit c: Connection): Int = {
    SQL"""delete
          from "frontpage"."department"
          where "id" = ANY(${ids})
       """.executeUpdate()
    
  }
  override def insert(unsaved: DepartmentRow)(implicit c: Connection): DepartmentRow = {
    SQL"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          values (${ParameterValue(unsaved.id, null, DepartmentId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}, ${ParameterValue(unsaved.budget, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric, ${ParameterValue(unsaved.companyId, null, ToStatement.optionToStatement(CompanyId.toStatement, CompanyId.parameterMetadata))}::uuid)
          returning "id", "name", "budget", "company_id"
       """
      .executeInsert(DepartmentRow.rowParser(1).single)
    
  }
  override def insert(unsaved: DepartmentRowUnsaved)(implicit c: Connection): DepartmentRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      Some((NamedParameter("budget", ParameterValue(unsaved.budget, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))), "::numeric")),
      Some((NamedParameter("company_id", ParameterValue(unsaved.companyId, null, ToStatement.optionToStatement(CompanyId.toStatement, CompanyId.parameterMetadata))), "::uuid")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, DepartmentId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."department" default values
            returning "id", "name", "budget", "company_id"
         """
        .executeInsert(DepartmentRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."department"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
                  values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
                  returning "id", "name", "budget", "company_id"
               """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(DepartmentRow.rowParser(1).single)
    }
    
  }
  override def insertStreaming(unsaved: Iterator[DepartmentRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."department"("id", "name", "budget", "company_id") FROM STDIN""", batchSize, unsaved)(DepartmentRow.text, c)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[DepartmentRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."department"("name", "budget", "company_id", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(DepartmentRowUnsaved.text, c)
  }
  override def select: SelectBuilder[DepartmentFields, DepartmentRow] = {
    SelectBuilderSql(""""frontpage"."department"""", DepartmentFields.structure, DepartmentRow.rowParser)
  }
  override def selectAll(implicit c: Connection): List[DepartmentRow] = {
    SQL"""select "id", "name", "budget", "company_id"
          from "frontpage"."department"
       """.as(DepartmentRow.rowParser(1).*)
  }
  override def selectById(id: DepartmentId)(implicit c: Connection): Option[DepartmentRow] = {
    SQL"""select "id", "name", "budget", "company_id"
          from "frontpage"."department"
          where "id" = ${ParameterValue(id, null, DepartmentId.toStatement)}
       """.as(DepartmentRow.rowParser(1).singleOpt)
  }
  override def selectByIds(ids: Array[DepartmentId])(implicit c: Connection): List[DepartmentRow] = {
    SQL"""select "id", "name", "budget", "company_id"
          from "frontpage"."department"
          where "id" = ANY(${ids})
       """.as(DepartmentRow.rowParser(1).*)
    
  }
  override def selectByIdsTracked(ids: Array[DepartmentId])(implicit c: Connection): Map[DepartmentId, DepartmentRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[DepartmentFields, DepartmentRow] = {
    UpdateBuilder(""""frontpage"."department"""", DepartmentFields.structure, DepartmentRow.rowParser)
  }
  override def update(row: DepartmentRow)(implicit c: Connection): Boolean = {
    val id = row.id
    SQL"""update "frontpage"."department"
          set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)},
              "budget" = ${ParameterValue(row.budget, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric,
              "company_id" = ${ParameterValue(row.companyId, null, ToStatement.optionToStatement(CompanyId.toStatement, CompanyId.parameterMetadata))}::uuid
          where "id" = ${ParameterValue(id, null, DepartmentId.toStatement)}
       """.executeUpdate() > 0
  }
  override def upsert(unsaved: DepartmentRow)(implicit c: Connection): DepartmentRow = {
    SQL"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          values (
            ${ParameterValue(unsaved.id, null, DepartmentId.toStatement)}::uuid,
            ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)},
            ${ParameterValue(unsaved.budget, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric,
            ${ParameterValue(unsaved.companyId, null, ToStatement.optionToStatement(CompanyId.toStatement, CompanyId.parameterMetadata))}::uuid
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "budget" = EXCLUDED."budget",
            "company_id" = EXCLUDED."company_id"
          returning "id", "name", "budget", "company_id"
       """
      .executeInsert(DepartmentRow.rowParser(1).single)
    
  }
  override def upsertBatch(unsaved: Iterable[DepartmentRow])(implicit c: Connection): List[DepartmentRow] = {
    def toNamedParameter(row: DepartmentRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, DepartmentId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement)),
      NamedParameter("budget", ParameterValue(row.budget, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))),
      NamedParameter("company_id", ParameterValue(row.companyId, null, ToStatement.optionToStatement(CompanyId.toStatement, CompanyId.parameterMetadata)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
                values ({id}::uuid, {name}, {budget}::numeric, {company_id}::uuid)
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name",
                  "budget" = EXCLUDED."budget",
                  "company_id" = EXCLUDED."company_id"
                returning "id", "name", "budget", "company_id"
             """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(DepartmentRow.rowParser(1).*)
    }
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[DepartmentRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    SQL"""create temporary table department_TEMP (like "frontpage"."department") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy department_TEMP("id", "name", "budget", "company_id") from stdin""", batchSize, unsaved)(DepartmentRow.text, c): @nowarn
    SQL"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          select * from department_TEMP
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "budget" = EXCLUDED."budget",
            "company_id" = EXCLUDED."company_id"
          ;
          drop table department_TEMP;""".executeUpdate()
  }
}
