/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package employee

import adventureworks.customtypes.Defaulted
import adventureworks.frontpage.person.PersonId
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterMetaData
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.SqlStringInterpolation
import anorm.ToStatement
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class EmployeeRepoImpl extends EmployeeRepo {
  override def delete: DeleteBuilder[EmployeeFields, EmployeeRow] = {
    DeleteBuilder(""""frontpage"."employee"""", EmployeeFields.structure)
  }
  override def deleteById(id: EmployeeId)(implicit c: Connection): Boolean = {
    SQL"""delete from "frontpage"."employee" where "id" = ${ParameterValue(id, null, EmployeeId.toStatement)}""".executeUpdate() > 0
  }
  override def deleteByIds(ids: Array[EmployeeId])(implicit c: Connection): Int = {
    SQL"""delete
          from "frontpage"."employee"
          where "id" = ANY(${ids})
       """.executeUpdate()
    
  }
  override def insert(unsaved: EmployeeRow)(implicit c: Connection): EmployeeRow = {
    SQL"""insert into "frontpage"."employee"("id", "person_id", "salary")
          values (${ParameterValue(unsaved.id, null, EmployeeId.toStatement)}::uuid, ${ParameterValue(unsaved.personId, null, PersonId.toStatement)}::uuid, ${ParameterValue(unsaved.salary, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric)
          returning "id", "person_id", "salary"
       """
      .executeInsert(EmployeeRow.rowParser(1).single)
    
  }
  override def insert(unsaved: EmployeeRowUnsaved)(implicit c: Connection): EmployeeRow = {
    val namedParameters = List(
      Some((NamedParameter("person_id", ParameterValue(unsaved.personId, null, PersonId.toStatement)), "::uuid")),
      Some((NamedParameter("salary", ParameterValue(unsaved.salary, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))), "::numeric")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, EmployeeId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."employee" default values
            returning "id", "person_id", "salary"
         """
        .executeInsert(EmployeeRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."employee"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
                  values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
                  returning "id", "person_id", "salary"
               """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(EmployeeRow.rowParser(1).single)
    }
    
  }
  override def insertStreaming(unsaved: Iterator[EmployeeRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."employee"("id", "person_id", "salary") FROM STDIN""", batchSize, unsaved)(EmployeeRow.text, c)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[EmployeeRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."employee"("person_id", "salary", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(EmployeeRowUnsaved.text, c)
  }
  override def select: SelectBuilder[EmployeeFields, EmployeeRow] = {
    SelectBuilderSql(""""frontpage"."employee"""", EmployeeFields.structure, EmployeeRow.rowParser)
  }
  override def selectAll(implicit c: Connection): List[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
          from "frontpage"."employee"
       """.as(EmployeeRow.rowParser(1).*)
  }
  override def selectById(id: EmployeeId)(implicit c: Connection): Option[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
          from "frontpage"."employee"
          where "id" = ${ParameterValue(id, null, EmployeeId.toStatement)}
       """.as(EmployeeRow.rowParser(1).singleOpt)
  }
  override def selectByIds(ids: Array[EmployeeId])(implicit c: Connection): List[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
          from "frontpage"."employee"
          where "id" = ANY(${ids})
       """.as(EmployeeRow.rowParser(1).*)
    
  }
  override def selectByIdsTracked(ids: Array[EmployeeId])(implicit c: Connection): Map[EmployeeId, EmployeeRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def selectByUniquePersonId(personId: PersonId)(implicit c: Connection): Option[EmployeeRow] = {
    SQL"""select "id", "person_id", "salary"
          from "frontpage"."employee"
          where "person_id" = ${ParameterValue(personId, null, PersonId.toStatement)}
       """.as(EmployeeRow.rowParser(1).singleOpt)
    
  }
  override def update: UpdateBuilder[EmployeeFields, EmployeeRow] = {
    UpdateBuilder(""""frontpage"."employee"""", EmployeeFields.structure, EmployeeRow.rowParser)
  }
  override def update(row: EmployeeRow)(implicit c: Connection): Boolean = {
    val id = row.id
    SQL"""update "frontpage"."employee"
          set "person_id" = ${ParameterValue(row.personId, null, PersonId.toStatement)}::uuid,
              "salary" = ${ParameterValue(row.salary, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric
          where "id" = ${ParameterValue(id, null, EmployeeId.toStatement)}
       """.executeUpdate() > 0
  }
  override def upsert(unsaved: EmployeeRow)(implicit c: Connection): EmployeeRow = {
    SQL"""insert into "frontpage"."employee"("id", "person_id", "salary")
          values (
            ${ParameterValue(unsaved.id, null, EmployeeId.toStatement)}::uuid,
            ${ParameterValue(unsaved.personId, null, PersonId.toStatement)}::uuid,
            ${ParameterValue(unsaved.salary, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData))}::numeric
          )
          on conflict ("id")
          do update set
            "person_id" = EXCLUDED."person_id",
            "salary" = EXCLUDED."salary"
          returning "id", "person_id", "salary"
       """
      .executeInsert(EmployeeRow.rowParser(1).single)
    
  }
  override def upsertBatch(unsaved: Iterable[EmployeeRow])(implicit c: Connection): List[EmployeeRow] = {
    def toNamedParameter(row: EmployeeRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, EmployeeId.toStatement)),
      NamedParameter("person_id", ParameterValue(row.personId, null, PersonId.toStatement)),
      NamedParameter("salary", ParameterValue(row.salary, null, ToStatement.optionToStatement(ToStatement.scalaBigDecimalToStatement, ParameterMetaData.BigDecimalParameterMetaData)))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."employee"("id", "person_id", "salary")
                values ({id}::uuid, {person_id}::uuid, {salary}::numeric)
                on conflict ("id")
                do update set
                  "person_id" = EXCLUDED."person_id",
                  "salary" = EXCLUDED."salary"
                returning "id", "person_id", "salary"
             """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(EmployeeRow.rowParser(1).*)
    }
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[EmployeeRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    SQL"""create temporary table employee_TEMP (like "frontpage"."employee") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy employee_TEMP("id", "person_id", "salary") from stdin""", batchSize, unsaved)(EmployeeRow.text, c): @nowarn
    SQL"""insert into "frontpage"."employee"("id", "person_id", "salary")
          select * from employee_TEMP
          on conflict ("id")
          do update set
            "person_id" = EXCLUDED."person_id",
            "salary" = EXCLUDED."salary"
          ;
          drop table employee_TEMP;""".executeUpdate()
  }
}
