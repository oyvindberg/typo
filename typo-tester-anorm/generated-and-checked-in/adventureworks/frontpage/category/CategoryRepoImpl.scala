/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package category

import adventureworks.customtypes.Defaulted
import anorm.BatchSql
import anorm.NamedParameter
import anorm.ParameterValue
import anorm.RowParser
import anorm.SQL
import anorm.SimpleSql
import anorm.SqlStringInterpolation
import anorm.ToStatement
import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class CategoryRepoImpl extends CategoryRepo {
  override def delete: DeleteBuilder[CategoryFields, CategoryRow] = {
    DeleteBuilder(""""frontpage"."category"""", CategoryFields.structure)
  }
  override def deleteById(id: CategoryId)(implicit c: Connection): Boolean = {
    SQL"""delete from "frontpage"."category" where "id" = ${ParameterValue(id, null, CategoryId.toStatement)}""".executeUpdate() > 0
  }
  override def deleteByIds(ids: Array[CategoryId])(implicit c: Connection): Int = {
    SQL"""delete
          from "frontpage"."category"
          where "id" = ANY(${ids})
       """.executeUpdate()
    
  }
  override def insert(unsaved: CategoryRow)(implicit c: Connection): CategoryRow = {
    SQL"""insert into "frontpage"."category"("id", "name")
          values (${ParameterValue(unsaved.id, null, CategoryId.toStatement)}::uuid, ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)})
          returning "id", "name"
       """
      .executeInsert(CategoryRow.rowParser(1).single)
    
  }
  override def insert(unsaved: CategoryRowUnsaved)(implicit c: Connection): CategoryRow = {
    val namedParameters = List(
      Some((NamedParameter("name", ParameterValue(unsaved.name, null, ToStatement.stringToStatement)), "")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((NamedParameter("id", ParameterValue(value, null, CategoryId.toStatement)), "::uuid"))
      }
    ).flatten
    val quote = '"'.toString
    if (namedParameters.isEmpty) {
      SQL"""insert into "frontpage"."category" default values
            returning "id", "name"
         """
        .executeInsert(CategoryRow.rowParser(1).single)
    } else {
      val q = s"""insert into "frontpage"."category"(${namedParameters.map{case (x, _) => quote + x.name + quote}.mkString(", ")})
                  values (${namedParameters.map{ case (np, cast) => s"{${np.name}}$cast"}.mkString(", ")})
                  returning "id", "name"
               """
      SimpleSql(SQL(q), namedParameters.map { case (np, _) => np.tupled }.toMap, RowParser.successful)
        .executeInsert(CategoryRow.rowParser(1).single)
    }
    
  }
  override def insertStreaming(unsaved: Iterator[CategoryRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."category"("id", "name") FROM STDIN""", batchSize, unsaved)(CategoryRow.text, c)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[CategoryRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    streamingInsert(s"""COPY "frontpage"."category"("name", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""", batchSize, unsaved)(CategoryRowUnsaved.text, c)
  }
  override def select: SelectBuilder[CategoryFields, CategoryRow] = {
    SelectBuilderSql(""""frontpage"."category"""", CategoryFields.structure, CategoryRow.rowParser)
  }
  override def selectAll(implicit c: Connection): List[CategoryRow] = {
    SQL"""select "id", "name"
          from "frontpage"."category"
       """.as(CategoryRow.rowParser(1).*)
  }
  override def selectById(id: CategoryId)(implicit c: Connection): Option[CategoryRow] = {
    SQL"""select "id", "name"
          from "frontpage"."category"
          where "id" = ${ParameterValue(id, null, CategoryId.toStatement)}
       """.as(CategoryRow.rowParser(1).singleOpt)
  }
  override def selectByIds(ids: Array[CategoryId])(implicit c: Connection): List[CategoryRow] = {
    SQL"""select "id", "name"
          from "frontpage"."category"
          where "id" = ANY(${ids})
       """.as(CategoryRow.rowParser(1).*)
    
  }
  override def selectByIdsTracked(ids: Array[CategoryId])(implicit c: Connection): Map[CategoryId, CategoryRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[CategoryFields, CategoryRow] = {
    UpdateBuilder(""""frontpage"."category"""", CategoryFields.structure, CategoryRow.rowParser)
  }
  override def update(row: CategoryRow)(implicit c: Connection): Boolean = {
    val id = row.id
    SQL"""update "frontpage"."category"
          set "name" = ${ParameterValue(row.name, null, ToStatement.stringToStatement)}
          where "id" = ${ParameterValue(id, null, CategoryId.toStatement)}
       """.executeUpdate() > 0
  }
  override def upsert(unsaved: CategoryRow)(implicit c: Connection): CategoryRow = {
    SQL"""insert into "frontpage"."category"("id", "name")
          values (
            ${ParameterValue(unsaved.id, null, CategoryId.toStatement)}::uuid,
            ${ParameterValue(unsaved.name, null, ToStatement.stringToStatement)}
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name"
          returning "id", "name"
       """
      .executeInsert(CategoryRow.rowParser(1).single)
    
  }
  override def upsertBatch(unsaved: Iterable[CategoryRow])(implicit c: Connection): List[CategoryRow] = {
    def toNamedParameter(row: CategoryRow): List[NamedParameter] = List(
      NamedParameter("id", ParameterValue(row.id, null, CategoryId.toStatement)),
      NamedParameter("name", ParameterValue(row.name, null, ToStatement.stringToStatement))
    )
    unsaved.toList match {
      case Nil => Nil
      case head :: rest =>
        new anorm.adventureworks.ExecuteReturningSyntax.Ops(
          BatchSql(
            s"""insert into "frontpage"."category"("id", "name")
                values ({id}::uuid, {name})
                on conflict ("id")
                do update set
                  "name" = EXCLUDED."name"
                returning "id", "name"
             """,
            toNamedParameter(head),
            rest.map(toNamedParameter)*
          )
        ).executeReturning(CategoryRow.rowParser(1).*)
    }
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[CategoryRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    SQL"""create temporary table category_TEMP (like "frontpage"."category") on commit drop""".execute(): @nowarn
    streamingInsert(s"""copy category_TEMP("id", "name") from stdin""", batchSize, unsaved)(CategoryRow.text, c): @nowarn
    SQL"""insert into "frontpage"."category"("id", "name")
          select * from category_TEMP
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name"
          ;
          drop table category_TEMP;""".executeUpdate()
  }
}
