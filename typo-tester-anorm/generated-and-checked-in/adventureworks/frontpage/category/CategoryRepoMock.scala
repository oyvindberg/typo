/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package category

import java.sql.Connection
import scala.annotation.nowarn
import typo.dsl.DeleteBuilder
import typo.dsl.DeleteBuilder.DeleteBuilderMock
import typo.dsl.DeleteParams
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderMock
import typo.dsl.SelectParams
import typo.dsl.UpdateBuilder
import typo.dsl.UpdateBuilder.UpdateBuilderMock
import typo.dsl.UpdateParams

class CategoryRepoMock(toRow: Function1[CategoryRowUnsaved, CategoryRow],
                       map: scala.collection.mutable.Map[CategoryId, CategoryRow] = scala.collection.mutable.Map.empty) extends CategoryRepo {
  override def delete: DeleteBuilder[CategoryFields, CategoryRow] = {
    DeleteBuilderMock(DeleteParams.empty, CategoryFields.structure, map)
  }
  override def deleteById(id: CategoryId)(implicit c: Connection): Boolean = {
    map.remove(id).isDefined
  }
  override def deleteByIds(ids: Array[CategoryId])(implicit c: Connection): Int = {
    ids.map(id => map.remove(id)).count(_.isDefined)
  }
  override def insert(unsaved: CategoryRow)(implicit c: Connection): CategoryRow = {
    val _ = if (map.contains(unsaved.id))
      sys.error(s"id ${unsaved.id} already exists")
    else
      map.put(unsaved.id, unsaved)
    
    unsaved
  }
  override def insert(unsaved: CategoryRowUnsaved)(implicit c: Connection): CategoryRow = {
    insert(toRow(unsaved))
  }
  override def insertStreaming(unsaved: Iterator[CategoryRow], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Iterator[CategoryRowUnsaved], batchSize: Int = 10000)(implicit c: Connection): Long = {
    unsaved.foreach { unsavedRow =>
      val row = toRow(unsavedRow)
      map += (row.id -> row)
    }
    unsaved.size.toLong
  }
  override def select: SelectBuilder[CategoryFields, CategoryRow] = {
    SelectBuilderMock(CategoryFields.structure, () => map.values.toList, SelectParams.empty)
  }
  override def selectAll(implicit c: Connection): List[CategoryRow] = {
    map.values.toList
  }
  override def selectById(id: CategoryId)(implicit c: Connection): Option[CategoryRow] = {
    map.get(id)
  }
  override def selectByIds(ids: Array[CategoryId])(implicit c: Connection): List[CategoryRow] = {
    ids.flatMap(map.get).toList
  }
  override def selectByIdsTracked(ids: Array[CategoryId])(implicit c: Connection): Map[CategoryId, CategoryRow] = {
    val byId = selectByIds(ids).view.map(x => (x.id, x)).toMap
    ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
  }
  override def update: UpdateBuilder[CategoryFields, CategoryRow] = {
    UpdateBuilderMock(UpdateParams.empty, CategoryFields.structure, map)
  }
  override def update(row: CategoryRow)(implicit c: Connection): Boolean = {
    map.get(row.id) match {
      case Some(`row`) => false
      case Some(_) =>
        map.put(row.id, row): @nowarn
        true
      case None => false
    }
  }
  override def upsert(unsaved: CategoryRow)(implicit c: Connection): CategoryRow = {
    map.put(unsaved.id, unsaved): @nowarn
    unsaved
  }
  override def upsertBatch(unsaved: Iterable[CategoryRow])(implicit c: Connection): List[CategoryRow] = {
    unsaved.map { row =>
      map += (row.id -> row)
      row
    }.toList
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Iterator[CategoryRow], batchSize: Int = 10000)(implicit c: Connection): Int = {
    unsaved.foreach { row =>
      map += (row.id -> row)
    }
    unsaved.size
  }
}
