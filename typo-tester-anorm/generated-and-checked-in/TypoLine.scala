/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks

import anorm.Column
import anorm.MetaDataItem
import anorm.ParameterMetaData
import anorm.SqlRequestError
import anorm.ToStatement
import anorm.TypeDoesNotMatch
import java.sql.PreparedStatement
import java.sql.Types
import org.postgresql.geometric.PGline
import org.postgresql.jdbc.PgArray
import play.api.libs.json.JsObject
import play.api.libs.json.JsResult
import play.api.libs.json.JsValue
import play.api.libs.json.Json
import play.api.libs.json.OFormat
import scala.util.Try

/** This implements a line represented by the linear equation Ax + By + C = 0 */
case class TypoLine(a: Double, b: Double, c: Double)
object TypoLine {
  implicit val oFormat: OFormat[TypoLine] = new OFormat[TypoLine]{
    override def writes(o: TypoLine): JsObject =
      Json.obj(
        "a" -> o.a,
        "b" -> o.b,
        "c" -> o.c
      )
  
    override def reads(json: JsValue): JsResult[TypoLine] = {
      JsResult.fromTry(
        Try(
          TypoLine(
            a = json.\("a").as[Double],
            b = json.\("b").as[Double],
            c = json.\("c").as[Double]
          )
        )
      )
    }
  }
  implicit val TypoLineDb: ToStatement[TypoLine] with ParameterMetaData[TypoLine] with Column[TypoLine] = new ToStatement[TypoLine] with ParameterMetaData[TypoLine] with Column[TypoLine] {
    override def sqlType: String = "line"
    override def jdbcType: Int = Types.OTHER
    override def set(s: PreparedStatement, index: Int, v: TypoLine): Unit =
      s.setObject(index, new PGline(v.a, v.b, v.c))
    override def apply(v: Any, v2: MetaDataItem): Either[SqlRequestError, TypoLine] =
      v match {
        case v: PGline => Right(TypoLine(v.a, v.b, v.c))
        case other => Left(TypeDoesNotMatch(s"Expected instance of PGline from JDBC to produce a TypoLine, got ${other.getClass.getName}"))
      }
  }
  
  implicit val TypoLineDbArray: ToStatement[Array[TypoLine]] with ParameterMetaData[Array[TypoLine]] with Column[Array[TypoLine]] = new ToStatement[Array[TypoLine]] with ParameterMetaData[Array[TypoLine]] with Column[Array[TypoLine]] {
    override def sqlType: String = "_line"
    override def jdbcType: Int = Types.ARRAY
    override def set(s: PreparedStatement, index: Int, v: Array[TypoLine]): Unit =
      s.setArray(index, s.getConnection.createArrayOf("line", v.map(v => new PGline(v.a, v.b, v.c))))
    override def apply(v: Any, v2: MetaDataItem): Either[SqlRequestError, Array[TypoLine]] =
      v match {
        case v: PgArray =>
         v.getArray match {
           case v: Array[_] =>
             Right(v.map(v => TypoLine(v.asInstanceOf[PGline].a, v.asInstanceOf[PGline].b, v.asInstanceOf[PGline].c)))
           case other => Left(TypeDoesNotMatch(s"Expected one-dimensional array from JDBC to produce an array of TypoLine, got ${other.getClass.getName}"))
         }
        case other => Left(TypeDoesNotMatch(s"Expected PgArray from JDBC to produce an array of TypoLine, got ${other.getClass.getName}"))
      }
  }

}
