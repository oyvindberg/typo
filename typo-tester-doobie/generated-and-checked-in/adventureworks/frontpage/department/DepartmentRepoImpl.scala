/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package frontpage
package department

import adventureworks.customtypes.Defaulted
import adventureworks.frontpage.company.CompanyId
import cats.instances.list.catsStdInstancesForList
import doobie.free.connection.ConnectionIO
import doobie.postgres.syntax.FragmentOps
import doobie.syntax.SqlInterpolator.SingleFragment.fromWrite
import doobie.syntax.string.toSqlInterpolator
import doobie.util.Write
import doobie.util.fragment.Fragment
import doobie.util.meta.Meta
import doobie.util.update.Update
import fs2.Stream
import typo.dsl.DeleteBuilder
import typo.dsl.SelectBuilder
import typo.dsl.SelectBuilderSql
import typo.dsl.UpdateBuilder

class DepartmentRepoImpl extends DepartmentRepo {
  override def delete: DeleteBuilder[DepartmentFields, DepartmentRow] = {
    DeleteBuilder(""""frontpage"."department"""", DepartmentFields.structure)
  }
  override def deleteById(id: DepartmentId): ConnectionIO[Boolean] = {
    sql"""delete from "frontpage"."department" where "id" = ${fromWrite(id)(new Write.Single(DepartmentId.put))}""".update.run.map(_ > 0)
  }
  override def deleteByIds(ids: Array[DepartmentId]): ConnectionIO[Int] = {
    sql"""delete from "frontpage"."department" where "id" = ANY(${ids})""".update.run
  }
  override def insert(unsaved: DepartmentRow): ConnectionIO[DepartmentRow] = {
    sql"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          values (${fromWrite(unsaved.id)(new Write.Single(DepartmentId.put))}::uuid, ${fromWrite(unsaved.name)(new Write.Single(Meta.StringMeta.put))}, ${fromWrite(unsaved.budget)(new Write.SingleOpt(Meta.ScalaBigDecimalMeta.put))}::numeric, ${fromWrite(unsaved.companyId)(new Write.SingleOpt(CompanyId.put))}::uuid)
          returning "id", "name", "budget", "company_id"
       """.query(using DepartmentRow.read).unique
  }
  override def insert(unsaved: DepartmentRowUnsaved): ConnectionIO[DepartmentRow] = {
    val fs = List(
      Some((Fragment.const0(s""""name""""), fr"${fromWrite(unsaved.name)(new Write.Single(Meta.StringMeta.put))}")),
      Some((Fragment.const0(s""""budget""""), fr"${fromWrite(unsaved.budget)(new Write.SingleOpt(Meta.ScalaBigDecimalMeta.put))}::numeric")),
      Some((Fragment.const0(s""""company_id""""), fr"${fromWrite(unsaved.companyId)(new Write.SingleOpt(CompanyId.put))}::uuid")),
      unsaved.id match {
        case Defaulted.UseDefault => None
        case Defaulted.Provided(value) => Some((Fragment.const0(s""""id""""), fr"${fromWrite(value: DepartmentId)(new Write.Single(DepartmentId.put))}::uuid"))
      }
    ).flatten
    
    val q = if (fs.isEmpty) {
      sql"""insert into "frontpage"."department" default values
            returning "id", "name", "budget", "company_id"
         """
    } else {
      val CommaSeparate = Fragment.FragmentMonoid.intercalate(fr", ")
      sql"""insert into "frontpage"."department"(${CommaSeparate.combineAllOption(fs.map { case (n, _) => n }).get})
            values (${CommaSeparate.combineAllOption(fs.map { case (_, f) => f }).get})
            returning "id", "name", "budget", "company_id"
         """
    }
    q.query(using DepartmentRow.read).unique
    
  }
  override def insertStreaming(unsaved: Stream[ConnectionIO, DepartmentRow], batchSize: Int = 10000): ConnectionIO[Long] = {
    new FragmentOps(sql"""COPY "frontpage"."department"("id", "name", "budget", "company_id") FROM STDIN""").copyIn(unsaved, batchSize)(using DepartmentRow.text)
  }
  /* NOTE: this functionality requires PostgreSQL 16 or later! */
  override def insertUnsavedStreaming(unsaved: Stream[ConnectionIO, DepartmentRowUnsaved], batchSize: Int = 10000): ConnectionIO[Long] = {
    new FragmentOps(sql"""COPY "frontpage"."department"("name", "budget", "company_id", "id") FROM STDIN (DEFAULT '__DEFAULT_VALUE__')""").copyIn(unsaved, batchSize)(using DepartmentRowUnsaved.text)
  }
  override def select: SelectBuilder[DepartmentFields, DepartmentRow] = {
    SelectBuilderSql(""""frontpage"."department"""", DepartmentFields.structure, DepartmentRow.read)
  }
  override def selectAll: Stream[ConnectionIO, DepartmentRow] = {
    sql"""select "id", "name", "budget", "company_id" from "frontpage"."department"""".query(using DepartmentRow.read).stream
  }
  override def selectById(id: DepartmentId): ConnectionIO[Option[DepartmentRow]] = {
    sql"""select "id", "name", "budget", "company_id" from "frontpage"."department" where "id" = ${fromWrite(id)(new Write.Single(DepartmentId.put))}""".query(using DepartmentRow.read).option
  }
  override def selectByIds(ids: Array[DepartmentId]): Stream[ConnectionIO, DepartmentRow] = {
    sql"""select "id", "name", "budget", "company_id" from "frontpage"."department" where "id" = ANY(${ids})""".query(using DepartmentRow.read).stream
  }
  override def selectByIdsTracked(ids: Array[DepartmentId]): ConnectionIO[Map[DepartmentId, DepartmentRow]] = {
    selectByIds(ids).compile.toList.map { rows =>
      val byId = rows.view.map(x => (x.id, x)).toMap
      ids.view.flatMap(id => byId.get(id).map(x => (id, x))).toMap
    }
  }
  override def update: UpdateBuilder[DepartmentFields, DepartmentRow] = {
    UpdateBuilder(""""frontpage"."department"""", DepartmentFields.structure, DepartmentRow.read)
  }
  override def update(row: DepartmentRow): ConnectionIO[Boolean] = {
    val id = row.id
    sql"""update "frontpage"."department"
          set "name" = ${fromWrite(row.name)(new Write.Single(Meta.StringMeta.put))},
              "budget" = ${fromWrite(row.budget)(new Write.SingleOpt(Meta.ScalaBigDecimalMeta.put))}::numeric,
              "company_id" = ${fromWrite(row.companyId)(new Write.SingleOpt(CompanyId.put))}::uuid
          where "id" = ${fromWrite(id)(new Write.Single(DepartmentId.put))}"""
      .update
      .run
      .map(_ > 0)
  }
  override def upsert(unsaved: DepartmentRow): ConnectionIO[DepartmentRow] = {
    sql"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          values (
            ${fromWrite(unsaved.id)(new Write.Single(DepartmentId.put))}::uuid,
            ${fromWrite(unsaved.name)(new Write.Single(Meta.StringMeta.put))},
            ${fromWrite(unsaved.budget)(new Write.SingleOpt(Meta.ScalaBigDecimalMeta.put))}::numeric,
            ${fromWrite(unsaved.companyId)(new Write.SingleOpt(CompanyId.put))}::uuid
          )
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "budget" = EXCLUDED."budget",
            "company_id" = EXCLUDED."company_id"
          returning "id", "name", "budget", "company_id"
       """.query(using DepartmentRow.read).unique
  }
  override def upsertBatch(unsaved: List[DepartmentRow]): Stream[ConnectionIO, DepartmentRow] = {
    Update[DepartmentRow](
      s"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
          values (?::uuid,?,?::numeric,?::uuid)
          on conflict ("id")
          do update set
            "name" = EXCLUDED."name",
            "budget" = EXCLUDED."budget",
            "company_id" = EXCLUDED."company_id"
          returning "id", "name", "budget", "company_id""""
    )(using DepartmentRow.write)
    .updateManyWithGeneratedKeys[DepartmentRow]("id", "name", "budget", "company_id")(unsaved)(using catsStdInstancesForList, DepartmentRow.read)
  }
  /* NOTE: this functionality is not safe if you use auto-commit mode! it runs 3 SQL statements */
  override def upsertStreaming(unsaved: Stream[ConnectionIO, DepartmentRow], batchSize: Int = 10000): ConnectionIO[Int] = {
    for {
      _ <- sql"""create temporary table department_TEMP (like "frontpage"."department") on commit drop""".update.run
      _ <- new FragmentOps(sql"""copy department_TEMP("id", "name", "budget", "company_id") from stdin""").copyIn(unsaved, batchSize)(using DepartmentRow.text)
      res <- sql"""insert into "frontpage"."department"("id", "name", "budget", "company_id")
                   select * from department_TEMP
                   on conflict ("id")
                   do update set
                     "name" = EXCLUDED."name",
                     "budget" = EXCLUDED."budget",
                     "company_id" = EXCLUDED."company_id"
                   ;
                   drop table department_TEMP;""".update.run
    } yield res
  }
}
