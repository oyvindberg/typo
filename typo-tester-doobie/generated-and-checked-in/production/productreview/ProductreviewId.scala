/**
 * File has been automatically generated by `typo`.
 *
 * IF YOU CHANGE THIS FILE YOUR CHANGES WILL BE OVERWRITTEN.
 */
package adventureworks
package production
package productreview

import doobie.Get
import doobie.Meta
import doobie.Put
import doobie.Read
import doobie.Write

/** Type for the primary key of table `production.productreview` */
case class ProductreviewId(value: Int) extends AnyVal
object ProductreviewId {
  implicit val ordering: Ordering[ProductreviewId] = Ordering.by(_.value)
  
  implicit val putArray: Put[Array[ProductreviewId]] = Meta[Array[Int]].put.contramap(_.map(_.value))
  implicit val getArray: Get[Array[ProductreviewId]] = Meta[Array[Int]].get.map(_.map(ProductreviewId.apply))
  implicit val put: Put[ProductreviewId] = Put[Int].contramap(_.value)
  implicit val get: Get[ProductreviewId] = Get[Int].map(ProductreviewId.apply)
  implicit val write: Write[ProductreviewId] = Write[Int].contramap(_.value)
  implicit val read: Read[ProductreviewId] = Read[Int].map(ProductreviewId.apply)
}
